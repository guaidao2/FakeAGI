# DESIGN — 过程选择（Process Selection）：语言 × 目标层的统一

> 状态：**设计约束定稿 + 第一、二阶段已实现 + C2 完成**（2026-08-01，经三轮外部讨论加固）
> 实现状态：
> - ✅ 第一阶段（独立控制器验证）：`core/process_selector.py`（可靠性在线估计
>   + argmax 选择 + 真机会成本）+ `test_process_selector.py`（V1-V4 验收 +
>   N1-N4 负对照，全部通过，n=5 含 std）
> - ✅ 第二阶段（完整主循环接入 + n=30）：`test_process_integration.py`（冒烟
>   6 项）+ `run_process_selection_study.py`（3 环境 × 30 seeds + 3 对照，
>   判定全部通过）+ `PREREGISTER_PROCESS_SELECTION.md`（预注册 + 实测）
> - ✅ C2 叠加态升级：`SuperpositionEstimator`（多假设+贝叶斯坍缩+置信度地板）
>   ——被教歪可逆验证 4 项全过 + n=30 回归全过（`test_c2_superposition.py`）
> 位置：路线 ②（统一）——E13 目标层 + P8b 语言意图收成一个机制：
> **说话 = 信息寻求的一种过程选择**（问路 vs 自己扫掠，目标不变，过程可换）。
> 设计约束来自三轮讨论：架构师分析 + 外部评审 + 朋友补刀（世界模型被教歪）。

## 0. 统一命题

**目标导向的行为选择**（而非"饿了说话"和"饿了搜索"两个散装机制）：

```
饥饿（落差 > 阈值）→ 目标"获得资源"激活
  → 过程选择（世界模型评估）：
     问路（语言）vs 扫掠（InfoSeeker）vs 记忆（空间记忆）
  → 选预期落差消解率最高的过程
  → 过程失败 → 落差未消解 → 切换过程（目标不变）
```

公理④ 精炼的机制形态：**落差驱动目标，世界模型驱动过程选择**。

## 1. 三条设计约束（必须同时满足）

### C1 问路必须有成本/噪声（防语言 shortcut）——**成本必须 intrinsic**
- 环境响应**不完美**：有噪声（答非所问）/ 延迟 / 失败率
- 若"问路永远优于扫掠"（有问必答且免费），系统永远学不到自己的搜索能力——
  等于给了一个隐形外部奖励（正确答案），撞公理"拒绝外部奖励"红线
- **⚠️ 成本实现红线（朋友补刀）**：成本**绝不能**用"每次问路扣 -1 分"（外部惩罚）——
  那恰恰 reintroduce 外部奖励信号，撞碎公理①④——**治了 shortcut，引入更大的背叛**
- **正确实现（intrinsic 成本）**：
  - 问路占 1 个 tick（这段时间无法自己搜 = **机会成本**）
  - 或消耗有限的"通信 token 预算"（系统资源约束，非外部打分）
  - 成本来自系统自己的资源约束，与落差机制同源，不自相矛盾
- **生物学对应**：真实世界社会信息有代价（被误导/延迟/竞争），
  "问"的代价让"自己学"保持竞争力——这才是过程选择存在的理由

### C2 可靠性估计必须在线更新且进世界模型（防"被教歪"）
- **查表排除**：过程选择不是环境策略表（"这环境问路好使"）——
  而是世界模型预测两个过程的预期落差消解率
- **隐式查表也要排除**：世界模型本身可能被教歪——
  早期问路总成功 → 模型学到"问路可靠性=1.0" → 永久偏好问路且无反例更新
- **解法（架构自洽）**：过程可靠性估计放进**叠加态世界模型**：
  ```
  分支 A："问路可靠"（听到方向 → 找到食物）
  分支 B："问路不可靠"（听到方向 → 找不到）
  早期全成功 → A 振幅高（和查表一样）
  第一次失败回答 → B 预测误差低 → B 振幅升 → 坍缩
  → 可靠性在线修正（失败→gap→坍缩回路）
  ```
- 现有雏形确认：`language_trust`（E10）/ `speak_trust`（P8b）
  已是"失败→trust↓→试探"的标量版——② 要升级为**叠加态分支**（非标量）

### C3 过程选择必须世界模型驱动（防"策略表退化"）
- 决策路径：`落差 → 世界模型预测每个过程的预期落差消解率 → argmax`
- 不是硬编码偏好（问路优先/扫掠优先）
- 依赖 C2 的可靠性估计（世界模型对"问路"的预测含其可靠性）

## 2. 验收标准（显式验证，不默认"世界模型会自己学"）

设计意图和实现经常背叛（E13 教训）——以下必须**显式验证**：

| 验证 | 内容 | 通过标准 | 实测（第一阶段）|
|------|------|---------|---------|
| V1 | 失败→gap→坍缩回路 | 环境连续给错方向 → 可靠性**必须下降** | ✅ 0.5→0.05（30 tick 全错）|
| V2 | 残差误差进模型 | 错误回答的预测误差确实进入更新（非被淹没）| ✅ 连续失败确实更新 |
| V3 | 可靠性可逆 | 恢复正确后可靠性能回升（非永久毒化）| ✅ 回升 1.0（经 5% 试探，约 2 次越阈）|
| V4 | **切换由可靠性引起**（强化）| 可靠性比较驱动选择翻向 | ✅ ask 优→选 ask，sweep 优→选 sweep（单元级）|

## 3. 负对照（证明"选择真由模型驱动"）——**作为对比阵列读，非独立组**

| 组 | 配置 | 预期 | 诊断力 | 实测（n=5 含 std）|
|----|------|------|--------|---------|
| N1 冻结可靠性 | 分支振幅/trust 锁死（不更新）| 表现差（证明在线更新必要）| **N1 vs N4**：若冻结后仍"选得对"→ 靠硬编码偏好，C2 破 | ✅ 200±0 vs N4 160±3 问路（冻结仍问——无法适应切换）|
| N2 问路永远失败 | 环境永远答非所问 | 系统学会"从不用问路" | **N2 vs N4 构成一对**：切换由可靠性估计驱动 → 选择是模型的 | ✅ 13±3 vs N3 179±35（对称族内显著少问）|
| N3 语言可用但噪声 | 响应有噪声（C1）| 学会"先试自己找，问不到再问" | N3 展示真过程选择 | ✅ 179±35（噪声下仍偏问，可接受）|
| N4 语言免费完美 | 有问必答 | 若纯问——可能是**正确行为** | N4 自身不能单独读；价值在对比 | ✅ 160±3（切换后学会 sweep）|

**对比阵列的读法**（朋友补刀）：
- N1–N4 不当四个独立组报，当一组对比阵列读
- 核心问题：**"可靠性估计的变化是否驱动了选择的变化"**
- N4 单独读无意义（纯问可能是正确行为）；必须与 N1/N2 对齐才显形
- 真正暴露泄漏的是 **N1（冻结）vs N4（免费）**：冻结后仍选得对 → C2 破

> ⚠️ N1–N4 是实验不是装饰：**n 标准与 E13 相同（n=30）**，
> n=3 的"问路永远失败组系统没问"可能只是 seed 噪声，读不出"学会了不问"。
> 预注册时把负对照的 n 和判定标准一并锁死。

## 4. 与路线 ①③ 的关系（防泄漏）

- **E13 结论与统一系统分开报**：E13 的 5 倍/1.4 倍是单模块证据（n=3）；
  统一系统可能改了搜索动力学（语言介入后信息寻求变化）——不可回填混用
- **① 并行偷跑**：E13 已 instrumented，n=30 跨环境预注册现在可跑
  （不依赖 ②），拿到"已统计验证"锚点
- **③ 无偏复杂环境**：环境 = "更复杂的世界"，不做"恰好需要四件套"的定制；
  消融验证（去掉语言还能解吗/去掉搜索呢）比"用了几个模块"更硬

## 5. 统计预注册（针对 peeking 教训）

n=30 跑前锁死（写入本文件，先于实验）：
- 效应量阈值：非仅 p<0.05，需明确 e.g. G6/G5 比值 ≥ 2 才算"机制级"
- 跨环境复现标准：≥ 2/3 环境复现同方向
- 塌陷预案：5 倍塌到不显著 → 如实报告 + 归因（不 reinterpret）

## 6. 实现顺序（② 内部）

```
1. ✅ 叠加态分支扩展：为"过程可靠性"建分支——【SuperpositionEstimator 已实现】
2. ✅ 过程选择器：世界模型预测各过程预期落差消解率 → argmax——【标量版已做】
3. ✅ 问路成本接入：环境响应噪声/延迟/失败率（C1）——【真机会成本+噪声】
4. ✅ V1-V4 验收脚本（显式断言）——【已通过】
5. ✅ N1-N4 负对照组——【已通过，n=5】
6. ✅ 接入完整 FakeAGI 主循环（替代独立控制器）——【冒烟 6 项 + n=30 全过】
7. ✅ n=30 预注册统计——【判定全过，见 PREREGISTER §7】
```

## 7. 明确不做（当前阶段）

- ❌ 目标层级生长（Maslow 式）
- ❌ 多目标冲突仲裁
- ❌ 过程选择的语言报告（"我选择问路因为..."——先让选择本身成立）
