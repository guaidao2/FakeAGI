# DESIGN — 概念层级（Concept Layer）：意义的单位

> 状态：设计讨论定稿（2026-08，讨论记录）
> 动机：连续三次"机制修复但宏观判定不变"（override 通路、元认知目标质量、
> sleep 集成）——诊断为学习信号空转（世界模型预测观测恒 0.5 兜底），
> 深挖后发现三层结构性问题：价值信号无锚、概念层级缺失、稳态僵化。
> 本文档是讨论产物（含自我修正），作为概念层级的最小可证伪实验设计。

## 0. 讨论中的自我修正（方法论纪律）

**错误表述**（已修正）："世界模型应该只预测身体价值，不预测环境"。
**修正**：人类确实预测世界（"明天会下雨"）——错不在"预测世界"，
错在**预测世界却没有价值锚**。世界模型仍预测观测，但需要价值函数
告诉它**哪些预测重要**（预测世界是手段，预测价值是目的）。

## 1. 三层诊断（由表及里）

```
表层：价值信号空转
  → 世界模型预测 obs'，误差恒 0.5（观测无价值结构可学）
  → GameNN TD 信号基于弱 reward（×10 的 -0.0005）
  → 元认知 gap 检测靠 fast_failure 兜底
  → 生长引擎 loss 停滞条件无意义
次层：稳态僵化（缺 Allostasis）
  → V = f(energy, water, health) 若固定，环境变化后旧价值锚不肯松手
  → 生物是预测性调节（应变稳态），不是恒定维持
  → 这解释 E6 规则变化适应波动大：不是找不到新水源，是旧锚不松
深层：概念层级缺失
  → 系统没有"概念"（食物/水/危险），只有观测向量
  → 语言无法接地（词没有"所指"）、推理无符号载体、
    抽象/迁移无单位（之前跨域实验失败正是没有概念层）
```

## 2. 概念的工程定义（不玄学）

**概念 = 观测簇 × 价值绑定 × 可重用**：
```
"食物"概念 = { 使 energy 上升的观测的聚类 }
            + 预测（靠近它 → V 上升）
            + 可重用（其他位置/时间认出它）
```
- 不是符号、不是向量——是**身体经验的压缩**（②活着 的产物）
- 来源：身体经验自发聚类，非外部标注（符合公理① 拒绝外部注入）
- 这是与主流监督式概念的根本区别

## 3. 四阶段路线

```
阶段 1：价值锚聚类（最小可证伪实验，~200 行）
  V 上升事件聚类成簇（concept_bank.py 已有骨架）
  验证：能"认出"未见过的食物形态（跨形态泛化）
  —— 之前概念实验失败根源是无价值锚：MLP 学像素簇学不动，
     学价值簇可行（可检验）

阶段 2：概念的预测绑定（接世界模型）
  每个概念预测自己的价值变化
  世界模型信号：预测观测 → 预测概念的价值
  验证：概念预测误差显著低于观测预测误差（学习有内容）

阶段 3：概念的符号化（接 P8c 语言）
  词 ↔ 概念双向绑定：听"食物"→激活食物概念；看食物簇→激活"食物"词
  验证：语言从"听词→动作反射"升级为"听词→激活概念→预测价值→动作"
  （可检验：词能改变概念激活，不只是动作）

阶段 4：概念的组合与自指（接自我模型）
  概念图（食物 ∪ 水源 → "可消耗物"抽象——迁移的真正单位）
  自我概念（身体状态簇 → "我饿了"的表征）
  → 意向性的可检验替代：系统能"解释自己"（为什么做 a → 概念解释链）
```

## 4. 方法论护栏（防"变味"成强化学习）

| 允许 | 禁止 |
|------|------|
| V = f(energy, water, health, 斜率)——身体物理量 | V = f(任务完成度, 食物数)——外部定义 |
| 固定映射，出生时给定（⑩ 价值注入初始化）| 随任务调参 |
| 世界模型**预测**价值变化（预测性）| 直接**追求**价值（目标性=RL）|
| V 影响学习系统（世界模型目标/GameNN TD/元认知）| V 直接决定动作（绕过驱动力/反射，违反⑥）|

**关键护栏**：价值函数只喂学习系统，不直接驱动动作——动作仍由
驱动力+反射主导（⑥ 耦合循环），学习系统通过预测价值逐渐学会
更好的行为。三层结构保持：身体决定方向（反射）、学习决定策略
（世界模型预测价值）、元认知决定探索（价值不确定区域）。

## 5. 公开的未知（诚实清单）

1. 价值锚聚类能否自发形成"有用的概念"（而非噪声簇）——阶段 1 检验
2. Allostasis（V 可塑性）是否必要先于概念层——若阶段 1 聚不出来，
   回退修 V 的可塑性（调整阈值随历史均值漂移）
3. 概念→词的绑定是否需要真正的语言输出通道（生成），还是仅激活即可
4. "意识"不可工程化验证（哲学死结）——只做"自我表征"（可检验替代）

## 6. 明确不做（当前阶段）

- 不引入外部监督标签（违反公理①）
- 不做 RL 式价值最大化（违反③ 的自主性）
- 不预先定义概念集合（必须从身体经验长出）
- 不声称"意识"——只验证"自我表征"与"概念泛化"
