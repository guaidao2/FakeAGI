# DESIGN — 概念层级（Concept Layer）：意义的单位

> 状态：设计讨论定稿（2026-08，讨论记录）
> 动机：连续三次"机制修复但宏观判定不变"（override 通路、元认知目标质量、
> sleep 集成）——诊断为学习信号空转（世界模型预测观测恒 0.5 兜底），
> 深挖后发现三层结构性问题：价值信号无锚、概念层级缺失、稳态僵化。
> 本文档是讨论产物（含自我修正），作为概念层级的最小可证伪实验设计。

## 0. 讨论中的自我修正（方法论纪律）

**错误表述**（已修正）："世界模型应该只预测身体价值，不预测环境"。
**修正**：人类确实预测世界（"明天会下雨"）——错不在"预测世界"，
错在**预测世界却没有价值锚**。世界模型仍预测观测，但需要价值函数
告诉它**哪些预测重要**（预测世界是手段，预测价值是目的）。

## 1. 三层诊断（由表及里）

```
表层：价值信号空转
  → 世界模型预测 obs'，误差恒 0.5（观测无价值结构可学）
  → GameNN TD 信号基于弱 reward（×10 的 -0.0005）
  → 元认知 gap 检测靠 fast_failure 兜底
  → 生长引擎 loss 停滞条件无意义
次层：稳态僵化（缺 Allostasis）
  → V = f(energy, water, health) 若固定，环境变化后旧价值锚不肯松手
  → 生物是预测性调节（应变稳态），不是恒定维持
  → 这解释 E6 规则变化适应波动大：不是找不到新水源，是旧锚不松
深层：概念层级缺失
  → 系统没有"概念"（食物/水/危险），只有观测向量
  → 语言无法接地（词没有"所指"）、推理无符号载体、
    抽象/迁移无单位（之前跨域实验失败正是没有概念层）
```

## 2. 概念的工程定义（不玄学）

**概念 = 观测簇 × 价值绑定 × 可重用**：
```
"食物"概念 = { 使 energy 上升的观测的聚类 }
            + 预测（靠近它 → V 上升）
            + 可重用（其他位置/时间认出它）
```
- 不是符号、不是向量——是**身体经验的压缩**（②活着 的产物）
- 来源：身体经验自发聚类，非外部标注（符合公理① 拒绝外部注入）
- 这是与主流监督式概念的根本区别

## 3. 四阶段路线

```
阶段 1：价值锚聚类（最小可证伪实验，~200 行）
  V 上升事件聚类成簇（concept_bank.py 已有骨架）
  验证：能"认出"未见过的食物形态（跨形态泛化）
  —— 之前概念实验失败根源是无价值锚：MLP 学像素簇学不动，
     学价值簇可行（可检验）
  ✅ 已完成（b5cf7be+41fe394：跨形态泛化 + 判别力双修复）
  ✅ 已接主循环（abffea4+a538327：consumable 簇 freq=146）
  ✅ 概念驱动行为（b6269c9+d192757：match_concept + 引导停留——
     n=10+Fisher 双侧定论：通路接通（B 停留 2.0）+ 中性净效果
     （A 持平 20.7 vs 20.8、D p≈0.49）——引导策略需更优设计）

阶段 2：概念的预测绑定（接世界模型）
  每个概念预测自己的价值变化
  世界模型信号：预测观测 → 预测概念的价值
  验证：概念预测误差显著低于观测预测误差（学习有内容）
  ✅ 已完成（8fa4bc0+6fe4f8f+aba7235）：Concept 价值 EMA +
     predict_value——test_concept_binding 判定 OK（高 0.842 vs
     低 0.328 跨概念区分）；**预测驱动引导（①）首次正收益：
     食物 20.7→23.0（+11%）**——概念层第一个真正有用的闭环

阶段 3：概念的符号化（接 P8c 语言）
  词 ↔ 概念双向绑定：听"食物"→激活食物概念；看食物簇→激活"食物"词
  验证：语言从"听词→动作反射"升级为"听词→激活概念→预测价值→动作"
  （可检验：词能改变概念激活，不只是动作）
  ✅ 已完成（44e6268+4e5bd9a+a5d7f89）：Concept.symbols + bind_symbol/
     activate_by_symbol + main.py 共现绑定（Hebbian）+ 符号激活
     引导——test_concept_symbol 判定 OK（food→consumable_0
     v=0.842「所指」+ water 不激活区分 + 符号-价值一致）；
     审查修复：符号路径接入语言信任闭环 + None 防护对称化
  ✅ 端到端（334a3f6+073073a）：听词→行为价值 +20% 食物
     （test_concept_language——词通过概念通路产生行为价值）
  ✅ 语言生成（2617be4）：ConceptBank.speak（概念→词）——
     test_concept_speak 判定 OK（双向语言闭环）
  ⏳ 多智能体协作（2617be4+9328553+c94b8f1）：盲听者+广播带
     方向——诚实负结果（协作 8 vs 独行 21——单轴方向导航不足+
     无终止条件；通道通 heard 427）——社会性价值待优化
  ✅ 社会智能四层优化（141ad75+57da4ee+48fa217）：① 他者信任
     （SpeakerTrust——信任绑定说话者）② 质疑能力（一次误导重罚）
     ③ 通信代价（token 预算）④ 双轴广播+投票合并——**协作 18 vs
     独行 1（18 倍）——语言社会性价值实证成立**

阶段 4：概念的组合与自指（接自我模型）
  概念图（食物 ∪ 水源 → "可消耗物"抽象——迁移的真正单位）
  自我概念（身体状态簇 → "我饿了"的表征）
  → 意向性的可检验替代：系统能"解释自己"（为什么做 a → 概念解释链）
  ✅ 已完成（7dbe796）：概念图抽象组（add_abstract_group/
     match_abstract——可消耗物=食物∪水源，跨子概念泛化+
     带价值+有边界，test_concept_compose 判定 OK）+ 自我概念
     （BodyStateCluster——饥饿/健康状态簇区分，质心 0.707，
     test_self_concept 判定 OK）——阶段 1-4 全部完成
  （② 端到端前置 334a3f6：词→行为价值 +20% 食物——
   「词为何要被理解」的直接实证）
```

## 4. 方法论护栏（防"变味"成强化学习）

| 允许 | 禁止 |
|------|------|
| V = f(energy, water, health, 斜率)——身体物理量 | V = f(任务完成度, 食物数)——外部定义 |
| 固定映射，出生时给定（⑩ 价值注入初始化）| 随任务调参 |
| 世界模型**预测**价值变化（预测性）| 直接**追求**价值（目标性=RL）|
| V 影响学习系统（世界模型目标/GameNN TD/元认知）| V 直接决定动作（绕过驱动力/反射，违反⑥）|

**关键护栏**：价值函数只喂学习系统，不直接驱动动作——动作仍由
驱动力+反射主导（⑥ 耦合循环），学习系统通过预测价值逐渐学会
更好的行为。三层结构保持：身体决定方向（反射）、学习决定策略
（世界模型预测价值）、元认知决定探索（价值不确定区域）。

**护栏裁决（a482958→6557aed，实证闭环）**：概念驱动初实现（匹配→
action=0 停留）与护栏字面存在张力（价值直驱动作）。n=10 双模式
对照裁决：**direct（直控）食物 23.0 存活 1/10 vs hint（概念投票
通道——价值经委员会加权仲裁）19.3 存活 4/10**——**hint 护栏内且
存活 4 倍（不锁定行为）→ 默认可切 hint，direct 保留开关**。
裁决含义：③ 动机选择器语义（概念参与决策而非替代决策）与护栏
字面可同时满足——"价值经决策层"是护栏的正确工程化。

## 5. 公开的未知（诚实清单）

1. 价值锚聚类能否自发形成"有用的概念"（而非噪声簇）——阶段 1 检验
2. Allostasis（V 可塑性）是否必要先于概念层——若阶段 1 聚不出来，
   回退修 V 的可塑性（调整阈值随历史均值漂移）
3. 概念→词的绑定是否需要真正的语言输出通道（生成），还是仅激活即可
4. "意识"不可工程化验证（哲学死结）——只做"自我表征"（可检验替代）

## 6. 明确不做（当前阶段）

- 不引入外部监督标签（违反公理①）
- 不做 RL 式价值最大化（违反③ 的自主性）
- 不预先定义概念集合（必须从身体经验长出）
- 不声称"意识"——只验证"自我表征"与"概念泛化"

## 7. 外部文献对照（2026-08 研究，含已验证 URL）

对四条补充线的研究结论——**共同发现：系统缺的不是机制元件
（world_model/replay/sleep/curiosity 都有雏形），而是接线**。

### 7.1 环境复杂度与可学习性（反身性前置审计）

- **SCAN 组合泛化**（Lake & Baroni 2018, arxiv.org/abs/1711.00350 ✅）：训练/测试差异小时只学
  mix-and-match 伪泛化——概念/抽象需要**可组合任务**才被迫形成
- **好奇心=learning progress 而非 novelty**（Pathak et al. 2017 ICM, arxiv.org/abs/1705.05363 ✅；
  Oudeyer & Kaplan 2007 类型学）：奖励应基于**预测误差下降率**，
  否则陷入 noisy-TV 噪声陷阱——本地 curiosity_map 是访问计数（AUDIT.md:32 已记录脱钩）
- **POMDP 迫使表征升级**（Kaelbling 1998）：部分可观察→必须维持隐状态→
  表征从"传感器读数"变"世界状态"——概念形成的压力源
- **最小可行**：环境加局部视野/遮挡 + 可组合复合目标；curiosity 预算改接
  world_model 误差下降率（learning progress），非计数

### 7.2 多时间尺度学习（比 Allostasis 更深）

- **互补学习系统 CLS**（McClelland et al. 1995, doi:10.1037/0033-295X.102.3.419 ⚠️）：
  快海马（episodic）+ 慢皮层（统计），回放桥接——本地 sleep 均匀抽样 replay
  未按显著性（sleep.py:36 `np.random.choice`）
- **meta-RL 快慢权重**（Wang et al. 2016, arxiv.org/abs/1611.05763 ✅）：
  慢速元参数在 RNN 快权重内实现快速适应
- **可塑性门控/敏感期**（Hubel & Wiesel 1970 ⚠️）：可塑性非恒定——
  本地可做"plasticity gate"：高应激/低能量冻结慢权重（稳态状态=发育窗口）
- **最小可行**：replay 按 prediction_gap 加权重放 + 世界模型慢副本（EMA）+ 稳态门控学习率

### 7.3 主动感知（Active Perception）

- **sensorimotor contingency**（O'Regan & Noë 2001, doi:10.1017/S0140525X01000115 ✅）：
  感知=对"动作如何改变感觉"规律的掌握，无内部图像
- **active inference epistemic value**（Parr & Friston 2017, royalsocietypublishing.org/doi/10.1098/rsif.2017.0376 ⚠️403；
  Friston 2010, nature.com/articles/nrn2787 ⚠️）：
  行动=降低不确定性（epistemic）与稳态（pragmatic）的统一
- **最小可行**：移动效用 = 稳态偏差项 + β·预期信息增益（预测误差下降方向）——
  saccade 与导航统一（本地注意力门控是加权，不是主动采样）

### 7.4 社会性学习与模仿（探索压缩）

- **社会学习策略开关**（Laland 2004, doi:10.3758/BF03196002 ✅）：
  copy when uncertain / copy majority / copy if better——模仿不能无差别
- **模仿先于语言**（Meltzoff & Moore 1977, science.org/doi/10.1126/science.198.4312.75 ⚠️超时）：
  新生儿即可模仿——概念形成的另一通道（语言之前是模仿）
- **运动变异性=目的性探索**（Dhawale et al. 2017, doi:10.1146/annurev-neuro-072116-031548 ✅）：
  模仿=把变异性导向他人已验证区域
- **最小可行**：演示缓冲（观测→动作）+ BC 辅助损失 + Laland 开关
  （仅当自身预测误差高或 teacher 更优时模仿）

### 7.5 综合结论（接线清单——缺的不是元件是连接）

| 现有元件 | 缺的接线 | 对应文献 |
|----------|----------|----------|
| curiosity_map（计数）| 接 world_model 误差下降率 | ICM / Oudeyer |
| curiosity 主循环零调用 | 接主循环（`_curiosity_lp_enabled` 门控）——**✅ B1 已接线（9610563，test_curiosity_lp.py A/B/C/D 全过；注：world_loss 变化 0.0000=信号空转实证 §8——接线已通，信号质量待世界模型修复）** | — |
| sleep consolidate（均匀抽样）| 按 prediction_gap 加权重放——**✅ B2 已接线（241b940，test_sleep_priority.py：高 surprise 27% vs 均匀 10% 基线）** | CLS |
| 单速率梯度更新 | 慢副本（EMA）+ 稳态门控学习率——**✅ B3 已接线（241b940 + 90e7c30，test_wm_slow.py A/B/C/D 全过：EMA 追踪/门控冻结 0.000000/生长重注册/cuda 惰性重注册；review blocking 修复：set_cognition 挂 agi 引用（门控原恒 1.0 死代码）+ EMA 设备惰性重注册）** | meta-RL / Hubel |
| 世界模型预测观测（恒 0.5 兜底）| **预测价值 V 水平——✅ A 步骤已接线（f3e44de+81cf190+acc237b+a49618e+41fe394+a538327，价值头基类+叠加态多假设坍缩；dv 观测时点差值+ΔV→V 水平稠密目标+归一化 energy/2 修复；验证：world_loss 恒 0.5→真实波动含奖励；收敛重验：value_mse 37x 下降但 B FAIL 学恒均值（收敛≠学对——判别力判据抓到伪象，输入信号 pred.detach() 待改进）；hidden 漂移独立课题）** | Active Inference / §8 信号空转 |
| AttentionGate（特征加权）| 移动效用加 epistemic 项——**🔄 B4 部分接线**（空间记忆引导 get_exploration_target 已实现——探索目标去未访问/高惊奇区域；learning progress 已接好奇心——"去最值得采样处"的方向性已具备；完整"移动效用=稳态+β·信息增益"待做） | active inference |
| hemin 影子自我（自我对比）| 演示缓冲 + BC + copy-when-uncertain——**⏳ B5 待 teacher 环境**（需多智能体/示范者提供（观测→动作）序列；主循环他者默认关，test_social 有雏形） | Laland / Meltzoff |
| 简单全可观察环境 | POMDP 化 + 组合任务——**✅ B 最小版已接线（4f99e3f，test_pomdp_env.py：隐藏状态开关解锁——t=1050 学会踩开关+吃食物，隐状态维持成功；概念层压力源建立）** | SCAN / Kaelbling |

**排序修正**：环境复杂度审计（7.1）是概念层的前置条件——若环境
不需要概念就能解，概念层不会涌现（苍蝇在只有水的盒子里不会进化导航）。

## 8. 诊断认识论：为什么是"接线"而非"重构"（2026-08 更新）

### 8.1 现象

连续三次"机制修复但宏观判定不变"（override 通路、元认知目标质量、
sleep 集成）。若只发生一次是巧合，三次是结构信号。

### 8.2 根因：一直在做零件测试，没做装配

- **30+ 模块每个单独有测试、单独通过**（world_model/LNN/GameNN/
  睡眠/语言/curiosity 各自验证 OK）
- **但主循环里接线断裂**（代码实况）：
  - `core/curiosity.py:17` — curiosity_map 是访问计数（`+1`），
    与世界模型预测误差完全脱钩（AUDIT.md:32 早记录未修）
  - `cognition/concept_bank.py:33` — ConceptBank 定义存在，
    主循环零调用（仅 persistence/dna 存取——又一个"存在未接线"）
  - `cognition/sleep.py:36` — consolidate 均匀抽样（`np.random.choice`），
    未按 prediction_gap 加权重放
  - 单速率梯度更新，无慢副本（EMA）、无可塑性门控
  - AttentionGate 是特征加权（被动），移动无 epistemic 项
  - hemin 影子自我只有自我对比，无观察-模仿通道
- **效果**：每个零件亮着灯，整机不转——学习信号根本没流到学习系统。
  修好每个环节的机制，但没修"信号本身"（世界模型预测观测恒 0.5 兜底
  = 信号空转）。

### 8.3 为什么不是重构（零件不是错的）

| 因素 | 说明 |
|------|------|
| 模块已验证 | 每个模块单独测试通过——不是坏的，是没连起来 |
| 实验资产保值 | E1-E14 判定、十一条公理验证链、双审查文化——它们验证"零件是对的"，结论依然成立 |
| 成本 | 装配比重铸低一个数量级，且每条接线可独立验证、可回滚 |
| 文献一致 | 四线研究（ICM/CLS/meta-RL/active inference/Laland）全是连接方案，不是重建方案 |

### 8.4 为什么环境和任务单调是前置

- 迷宫/网格：全可观察、线性因果、单一模态
- SCAN（Lake & Baroni 2018）：训练/测试差异小时只学 mix-and-match
  伪泛化——**统计关联就够的环境，概念层没有存在的压力**
- 结论：不是系统不聪明，是任务不配——先升级环境（POMDP 化 +
  组合任务），再装配，最后概念层才有涌现条件

### 8.5 一句话

**FakeAGI 是一台"零件齐全但没装配"的引擎**——零件测试不是白做
（否则装配时不知道哪坏了），只是卡在"零件测试→装配"之间。
研究给出装配图（§7.5 接线清单）与压力源（§7.1 环境升级），
缺的只是动手装配。

## 9. 全局工作空间（GWT）——"思维宫殿"的工程形式（2026-08 记录）

**问题来源**（墨渊安全实验室-coolmoon 提出）：人在思考时能将**所有知道的
东西穿插起来**（牛顿把苹果、月球轨道、引力定律串成一个解释），而当前语言
模型做不到——这是否是一个特殊的大脑工作机理？

**学术对应**：这是**全局工作空间理论（Global Workspace Theory, Baars）**——
意识/思考的本质 = 多个脑区的激活内容被**短暂广播到共享工作空间**，形成
"当前意识内容"；工作空间的内容再被各脑区消费（竞争-整合-再广播）。
LLM 的 attention 上下文窗口是**弱化版**工作空间（只能整合窗口内 token，
参数化知识无法自由穿插）；牛顿式思维需要**跨域知识全局整合**。

**与 FakeAGI 的差距（真实架构缺口）**：当前管线是**单向数据流**
（obs→LNN→世界模型→GameNN→决策），模块间只有"前向传递"，没有
**横向整合层**——概念库激活、空间记忆线索、价值预测、语言 token、
目标状态各自为政，永远不会"被同时拿到桌面上"。
这正是"思维宫殿"缺失的工程原因：不是没有知识，是没有**整合知识的
全局舞台**。

**设计方向（future work，不阻塞当前阶段）**：
1. **工作空间缓冲区**：每 tick 各模块把"激活内容"（概念激活向量/
   空间线索/价值预测/当前目标/惊奇）写入共享缓冲区 W(t)；
2. **广播-竞争**：W(t) 经可学习的整合器（类似 attention over modules）
   形成"当前思考状态"，再回投给各模块（决策/计划/概念激活调制）；
3. **与自模型（⑦）的关系**：自模型从"生存概率标量"升级为"工作空间
   状态的自指描述"——"我当前在想什么"= W(t) 的压缩表示；
4. **验证**：跨域穿插任务（如"看到水 + 记得口渴 + 语言词'喝'"三通道
   同时激活时决策改变）——需要 POMDP/多模态环境（§8.4）配套。

**为什么是"特殊机理"而不是"更大模型"**：LLM 的路是扩大参数化记忆 +
注意力窗口（广度），GWT 的路是**动态整合架构**（横向连接）——两条路
都通向"知识穿插"，但后者更像大脑的工作机理，且是 FakeAGI 模块化
架构的自然延伸（各模块已存在，缺的只是舞台）。

## 10. 工作纪律：每轮改动前必查"断线"（2026-08 记录）

**规则**：任何行为改动（系数/接线/机制）提交前，必须检查**是否引入了
断掉的线**——即：新接入的信号是否真的被消费、旧逻辑被改后是否有
残留调用点/死字段、门控开关是否真的生效。参照 §9 前接线审计
（B1-B3/W1-W5）的历史教训：模块单测全绿 ≠ 主循环数据真实流入。

**检查清单（每次改动后过一遍）**：
1. 新变量/新参数：是否有读取消费点（不是只写不读）？
2. 被修改的旧逻辑：旧字段/旧分支是否残留（死代码）？
3. 门控开关：默认值下新通路是否真的生效/真的关闭？
4. 数值改动：上下游消费方是否依赖旧数值（如系数 0.3→0.6 后
   dominance 可达性是否变化）？
5. 实验结果引用：改动是否影响已发布数字（需如实重测/标注）？
