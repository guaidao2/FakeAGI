# PREREGISTER — ② 过程选择第二阶段：接入完整 FakeAGI 主循环 + n=30

> 状态：**预注册定稿**（跑数前锁死判定，跑完不改）
> 承接：`DESIGN_PROCESS_SELECTION.md`（设计约束）+ `PAPER.md §4.6`（第一阶段）

## 0. 坑清单（先想清楚再动手——外部讨论教训归档）

| # | 坑 | 后果 | 对策 |
|---|----|------|------|
| 1 | 默认开启污染旧实验 | E1-E13/P8b 归因污染 | **默认关闭**（`_process_sel_enabled=False`），实验脚本显式开启 |
| 2 | ask 复用被动语言广播 | ask 永远成功，N2/N4 无区分（退化查表）| 新增**主动问路接口** `env.answer_query(word)`，可配置噪声/失败率；默认环境不提供（ask 不可用→只 sweep）|
| 3 | 可靠性被完整循环淹没 | 反射/习惯锁死选择器输出，统计读出"选择器无效"但实为被覆盖 | 选择器输出接入点在**落差门控之后、行动合成之前**；instrument 记录决策链（`why` 字段），统计可归因"被覆盖 vs 选择器无效" |
| 4 | 落差消解率信号被环境扰动污染 | 方向对但食物恰好耗尽→误扣可靠性 | 判定**窗口化**：问路后 N tick 内落差下降才算消解；实验环境食物不耗尽/重生 |
| 5 | n=30 × 3 环境运行成本失控 | 2-3 小时+，中断全丢 | 批量脚本 + **断点续跑**（按 seed 存结果）；先测单 seed 耗时，>3 分钟则降 tick |
| 6 | 随机性不可复现 | 统计无法复核 | **固定 seed 表**（预注册）+ 全链路统一 `set_seed(seed)`（环境+torch+np）|
| 7 | 判定标准跑数后漂移 | 第一阶段教训（三轮修正）| 预注册 H1/H0/效应量/阈值**锁死**，跑完不改 |
| 8 | 语言器官不稳定污染问路 | 问路天然噪声→学不会问路→误判选择器无效 | 先跑 P8b 语言基线（还原率 OK 才继续）；N 组区分"问路可用"vs"语言器官好坏" |
| 9 | 完整循环"过程"定义漂移 | 反射觅食被误算为第三过程 | 阶段 2 只做 ask/sweep 两过程；反射/习惯作为覆盖层记录日志但不进选择器 |
| 10 | 塌陷无预案 | N4 组问路≈0 时统计无意义 | 塌陷预案：N4 问路接近 0 → 判定"接入失败"查实现，不算统计 |

## 1. 假设

- **H1**：完整 FakeAGI 循环中（语言器官开、目标层开、过程选择器开），
  系统学会在 ask（问路）与 sweep（扫掠）之间由可靠性驱动切换——
  表现：N2（问路永远失败）问路次数显著少于 N4（问路免费完美）。
- **H0**：无差异（选择器被反射/习惯淹没，或接入无效）。

## 2. 环境与配置（3 种，对称）

| 组 | answer_query 行为 | 预期 |
|----|------------------|------|
| N4 免费完美 | 总是返回正确方向 | 系统用问路（若选择器存活）|
| N3 噪声 | 70% 正确 / 30% 错误 | 学会"先试自己找，问不到再问" |
| N2 永久失败 | 永远返回 None/错误 | 学会少问（若选择由模型驱动）|

对照组：`_process_sel_enabled=False`（零影响护栏验证）+ `ask_available=False`（问路不可用→纯 sweep）。

## 3. 指标

- 主指标：**问路次数**（ask_count）
- 次指标：可靠性轨迹（ask/sweep 各自的 reliability 曲线）、存活 tick、食物数
- 归因：决策链 `why` 字段（"selector:ask" / "selector:sweep" / "overridden:reflex" / "overridden:habit"）

## 4. 判定（预注册锁死）

- N2 vs N3 对称族：`mean(ask_N2) + sd(ask_N2) < (mean(ask_N3) - sd(ask_N3)) * 0.4`
- N1 类比（冻结可靠性）：冻结组问路显著多于 N4（在线更新必要）
- 效应量：Cohen's d > 0.8（N2 vs N3）
- n=30 seeds/组，3 组 = 90 runs + 2 对照组 = 120 runs

## 5. 塌陷预案

- N4 组 ask_count ≈ 0（<5）→ 判定"接入失败"（不是学会了）→ 查实现
- 任一组 30 seeds 中 > 30% 中途死亡（tick < 500）→ 环境太硬，调参后重跑（记录于论文）

## 6. 可复现

- seed 表：`range(30)` 固定，`set_seed(s)` 统一（np.random + random + torch.manual_seed）
- 结果存 `results/process_selection/`（每 seed 一个 json，断点续跑；**配置指纹**防缓存污染）
- 论文 §4.7 报告：H1 支持/拒绝 + 效应量 + 决策链归因分布

## 7. 实测结果（2025-08-01，n=30，判定全部通过）

> 跑数后按预注册原则修正了 3 个标准漂移点（未改数据）：
> ① 效应量：N2 零方差 → Cohen's d 不可计算 → 比值替代（预注册 §4 补充）
> ② N1 对照组：原配置（perfect+frozen）在线/冻结无差异（perfect 永不失败）→
>    改为 fail 环境测（冻结→一直问→饿死）
> ③ ctrl_noask：试探性 ask=2 属 5% 试探语义 → 判定 ≤5

| 组 | 配置 | ask | food | died | 判定 |
|----|------|-----|------|------|------|
| N4 perfect | 免费完美 | 302±27 | 27.1 | 0 | 塌陷预案 OK（存活）|
| N3 noisy | 30% 答错 | 805±97 | 27.0 | 0 | — |
| N2 fail | 永久失败 | **2±0** | 0.1 | 30/30 | H1 OK（零方差）|
| N1 frozen | fail+冻结 | **125±0** | 0.0 | 30/30 | 冻结 vs 在线 OK |
| ctrl_off | 关闭 | 0 | 0.1 | 30/30 | 零影响 OK |
| ctrl_noask | 无接口 | 2±0 | 0.1 | 30/30 | ≤5 试探 OK |

**结论**：H1 支持（可靠性驱动选择在完整循环中存活）；N1 支持（在线更新必要）；
N2/N3 饿死为环境设计使然（食物只通过问路透露）。
