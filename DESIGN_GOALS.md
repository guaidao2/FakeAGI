# DESIGN — 目标层（Goal Layer）：目标 vs 过程的分离

> 状态：设计定稿（2025-08-01）
> 动机：实验 12-S2（信息隐藏环境）FakeAGI 稳定失败——分析认为根因是
> **目标表征缺失**导致落差机制未激活（mis-specification），而非公理④ 失效。
> 本设计实现"目标 vs 过程"的显式分离，作为 S2 修复的可证伪实验（G2 组）。

## 0. 核心命题

**生命与自动机的分界 = 知道自己在追求什么（目标表征），且知道怎么做可以更换（过程选择）。**

- **目标（Goal）**：持久的状态期望（"我要维持 energy"），由身体结构内生
  （能量不足会死 = 物理约束，不是设计者奖励），**不因观测缺失而消失**。
- **过程（Process）**：达成目标的手段（反射/探索/问路/记忆），**可更换**。
- **落差（Gap）**：目标态 − 当前态 的显式内部变量 = 驱动信号。
  落差 → 信息寻求动机（epistemic value）→ 定向搜索（非随机）。

公理④ 精炼表述：
> 预测误差（落差）是内源驱动主信号；其激活以目标表征存在为前提；
> 目标表征由身体结构内生（活着就需要什么）。

## 1. 架构：三层结构

```
┌───────────────────────────────────────────┐
│ L0 存在层（已有）：body 内稳态               │ ← "活着"的物理约束
│     energy/water/integrity；energy<0.3 死亡  │
├───────────────────────────────────────────┤
│ L1 目标层（新增）：core/goals.py GoalState    │ ← "我要什么"显式表征
│     持久目标：维持 energy ≥ threshold         │
│     （长期记忆，不随观测变化）                 │
│     落差 gap = 目标态 − 当前态                │
│     信息寻求动机：gap 高 + 无线索 → 探索意图   │
├───────────────────────────────────────────┤
│ L2 过程层（已有）：决策委员会/GameNN/语言      │ ← "怎么做"（可更换）
│     觅食、探索、说话（P8b）                   │
│     目标不变，过程由世界模型选择               │
└───────────────────────────────────────────┘
```

## 2. GoalState 数据结构

```
class GoalState:
    goals: dict[str, Goal]     # 目标池（可生长）
    active_goal: str           # 当前主导目标
    gap: float                 # 主导目标落差（目标态−当前态）
    exploration_intent: float  # 信息寻求动机（落差驱动，0-1）
    history: list              # 目标达成记录（落差归零事件）

class Goal:
    name: str                  # "energy_maintenance"
    target_value: float        # 目标态（如 energy ≥ 0.8）
    current_fn: callable       # 当前态读取（body.energy）
    persistent: bool           # 持久性（不随观测消失）
    achieved: bool             # 落差是否归零
    urgency: float             # 紧迫度（target−current 归一化）
```

### 落差与信息寻求

```
gap = max(0, target − current)
exploration_intent = clamp(gap × weight, 0, 1)

当 exploration_intent 高 且 观测无资源线索：
  → 生成"搜索资源"意图（非坐标目标——"食物存在但位置未知"）
  → 定向探索：随机但**有记忆**（空间记忆标记已搜区域）
  → 找到线索/资源 → 落差部分消解 → 意图下降 → 收敛
```

关键：目标表征让系统**知道自己不知道什么**（"食物位置是未知量"），
从而把"随机"变成"定向搜索"——这是 RL ε-greedy 与落差驱动的本质区别。

## 3. 与现有模块的接缝

| 现有模块 | 接缝 |
|---------|------|
| `core/body.py` | Goal 的 current_fn 读取 body 状态（目标态来源=结构） |
| `core/drives.py` | drives 是隐式目标偏置；目标层将其**显式化**（drives 输出 → GoalState 更新） |
| `cognition/metacognition/` | GapDetector 新增"资源缺口"检测（饥饿+无线索 → 搜索目标） |
| `cognition/decision/committee.py` | exploration_intent 调制探索投票（定向而非随机） |
| `cognition/spatial_memory.py` | 搜索标记（已搜区域）→ 定向游走非重复 |

## 4. 可证伪消融（实验 13）

| 组 | 配置 | 预期 |
|----|------|------|
| G1 基线 | 当前 S2（无目标层）| 稳定失败（215tick 饿死）|
| G2 目标层 | 加 GoalState + 资源缺口搜索 | **定向搜索、找到食物、存活** |
| G3 RL+解锁奖励 | RL 加中间奖励 +0.5 | 分离 extrinsic 贡献 |
| G4 去反射+目标层 | G2 但禁用反射锚定 | 排除"反射锚定"混淆 |

判定：
- G2 收敛 → 支持"S2 是误测，公理④ 精炼成立"（落差为主信号，激活以目标先验为前提）
- G2 仍失败 → 更深的机制缺陷（落差→行动信用分配），需另定位

## 5. 明确不做（当前阶段）

- ❌ 目标层级生长（Maslow 式）——先验证单目标落差驱动
- ❌ 目标冲突仲裁（多目标竞争）——单目标先行
- ❌ 目标的语言报告（"我要食物"）——目标层稳定后再接 P7 说话
