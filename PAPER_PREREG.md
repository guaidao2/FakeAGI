# 预注册的可靠性建模：过程选择中的对照阵列与叠加态世界模型

**FakeAGI 分论文四**（总论文之外的独立实证论文）
总论文（理论基座）：[PAPER.md](PAPER.md)（十一公理体系与架构全貌）

玄幕安全团队 - guaidao2 · 墨渊安全实验室 - coolmoon · 2026-08

---

## 摘要

自组织智能体在"问路（ask）"与"扫掠（sweep）"两类信息获取过程间的选择，需要估计各过程的**可靠性**。本文报告一项**预注册**（pre-registered）的对照研究：n=30 统计判定，N1-N4 负对照阵列（冻结可靠性/永久失败/噪声/免费完美）+ V1-V4 验证组，检验"选择是否由在线更新的可靠性估计驱动"。结果显示：冻结可靠性组在失败环境仍频繁问路（200 vs 在线 16），证明在线更新必要；叠加态世界模型（多假设并存）在噪声条件下将问路次数从标量版的 390±149 降至 209±156。本文给出可靠性估计的贝叶斯形式化（多假设后验 + 置信度地板）与预注册判据的设计原则（零方差效应量需替代指标）。

**关键词**：预注册研究；可靠性估计；过程选择；叠加态世界模型；贝叶斯多假设

---

## 1. 引言

主动认知系统的核心问题之一：**何时信任内部模型，何时主动采样外部信息**（问路/扫掠）。FakeAGI 的 ② 过程选择模块（ProcessSelector）通过估计各过程的可靠性做出选择——但"选择是否真的由可靠性驱动"需要严格的对照验证，而非仅观察系统行为。

预注册（Nosek et al. 2018）是认知科学的黄金标准：先声明判据，再跑实验，防止事后合理化。本文将该标准应用于 FakeAGI 的过程选择模块。

**贡献**：
1. 预注册对照阵列设计（N1-N4 负对照 + V1-V4 验证——n=30）；
2. 在线可靠性更新的必要性证明（冻结 vs 在线对照）；
3. 叠加态世界模型（多假设贝叶斯）对噪声鲁棒性的改进；
4. 预注册设计中的方法论经验（零方差效应量问题）。

---

## 2. 系统概述

```
ProcessSelector：
  可靠性估计：r_ask（问路成功率——贝叶斯更新）
              r_sweep（扫掠成功率）
  选择：argmax(r_ask, r_sweep) + 5% 试探（探索）
  
SuperpositionEstimator（叠加态升级）：
  多假设并存：{r=0.9, r=0.5, r=0.1} 各带后验权重
  置信度地板：max × 1%（防单假设垄断）
  输出 = 后验加权均值

场景：问路（ask——成本 0.15/次）vs 扫掠（sweep——成本 0.05/次）
  N1 冻结：可靠性不更新（对照组）
  N2 失败：问路永远失败（应学会不用）
  N3 噪声：问路 30% 成功（应降低使用）
  N4 完美：问路免费且全对（应多用）
```

---

## 3. 数学推导

### 3.1 可靠性估计 = 贝叶斯后验

过程 p 的可靠性 r_p 以 Beta 后验建模：

$$r_p = \frac{\alpha_p + n_p^{成功}}{\alpha_p + \beta_p + n_p}$$

在线更新：每次问路消解（吃到）→ n^成功+1；失败 → n+1。

### 3.2 叠加态 = 多假设混合

单假设估计的弱点：初始先验决定终局（先验惰性——分论文一 §3.2）。叠加态维护 K 个假设：

$$p(r | D) = \sum_{k=1}^{K} w_k \cdot \text{Beta}(r; \alpha_k + n^{成功}, \beta_k + n)$$

其中假设 {0.9, 0.5, 0.1} 先验权重均匀，后验权重随证据更新：

$$w_k \propto \mathcal{L}(D | r_k) \cdot w_k^0$$

**置信度地板**：$\hat{w}_k = \max(w_k, \max_j w_j \times 0.01)$——防止单一假设垄断（"被教歪"的最后防线——多假设并存，不单点信任）。

**预测**：噪声条件下叠加态比标量估计更稳（低置信假设权重不崩——问路次数介于"信任"与"放弃"之间）。

### 3.3 预注册判据设计

判据设计原则（本文经验）：
- 效应量需非零方差（零方差效应量 → 比值替代——N1 冻结 200 vs 在线 2 用比值而非差值）；
- 负对照需隔离变量（N1 冻结 vs N4 完美的对比——若冻结后仍"选得对"→ 非在线更新驱动 → C2 破）。

---

## 4. 实验（预注册 n=30）

### 4.1 负对照阵列（N1-N4）

| 组 | 配置 | 问路次数（n=30）|
|----|------|----------------|
| N1 | 冻结可靠性 + 失败环境 | 200±0 |
| N2 | 在线更新 + 永久失败 | **16±3** |
| N3 | 在线更新 + 30% 噪声 | 198±1（标量版 390±149）|
| N4 | 在线更新 + 免费完美 | 302（应多用）|

**关键对比**：
- **N1 vs N4**：冻结后仍问路（200）——在线更新必要性（冻结=不学习→无法区分环境）；
- **N2 vs N3**：失败环境少问（16）vs 噪声环境多问（198）——**选择由可靠性估计驱动**（不是硬编码）。

### 4.2 验证组（V1-V4）+ 主循环集成

| 判定 | 结果 |
|------|------|
| A 零影响护栏（关闭时 ask=0）| OK |
| B 接入存活（perfect 组 ask>0）| OK（302）|
| D 消解率（success>fail）| OK（292 vs 10）|
| E 失败环境少问 | OK（fail 2 vs perfect 302）|
| F 切换（失败组 sweep>ask——argmax 翻盘）| OK（123 vs 2）|

**C 项（可靠性降幅 >0.1）**：fail 组可靠性 0.408→0.345（降幅 0.06）——**边缘 FAIL**（判据边界波动——通路工作、学习速率略降；如实记录不硬修）。

### 4.3 叠加态 vs 标量（噪声稳健性）

| 配置 | N3 噪声问路次数 |
|------|----------------|
| 标量可靠性 | 390±149 |
| 叠加态世界模型 | **209±156** |

**解读**：叠加态（多假设 + 置信度地板）将噪声条件下问路次数降至约一半——"被教歪"的最后防线实证（不单点信任）。

---

## 5. 讨论

### 5.1 核心结论

1. **在线更新必要**（N1 vs N4：冻结 200 vs 在线 16——不更新则无法适应失败环境）；
2. **选择由可靠性驱动**（N2 vs N3：失败 16 vs 噪声 198——区分环境类型）；
3. **叠加态提升噪声稳健性**（390→209——多假设并存）；
4. **预注册判据可行**：n=30 统计判定 + 零方差效应量替代方案（比值）——方法论贡献。

### 5.2 局限（诚实）

- C 项边缘 FAIL（降幅 0.06 < 0.1——判据边界波动，非机制失效）；
- 环境为 5×5 网格（问路/扫掠抽象）——真实环境（多模态信息源）未测；
- 假设数（3 个）与假设间距（0.9/0.5/0.1）为手工设定——自适应假设未实现；
- n=30 为组内统计（每组 30 次运行）——组间比较（N1 vs N4）未做多重比较校正。

### 5.3 与公理体系的关系

- 公理④（认识论子条款）——可靠性 = 对"过程可信度"的置信度估计（实证）；
- 公理②（活着）——零影响护栏（关闭时行为不变——防变味 RL）；
- 公理⑨（结构可塑性）——叠加态 = 多假设结构（非单点信任）。

---

## 6. 结论

预注册的对照阵列证明：FakeAGI 的过程选择**确实由在线更新的可靠性估计驱动**（N2 vs N3 环境区分），在线更新必要（N1 vs N4），叠加态世界模型提供噪声稳健性（390→209）。该研究是 AGI 圈稀缺的预注册实证——判据先声明、负对照阵列、边界如实记录。

---

## 参考文献

1. Nosek, B. A., et al. (2018). The preregistration revolution. *Proceedings of the National Academy of Sciences*, 115(11), 2600-2607.
2. Friston, K. (2010). The free-energy principle: a unified brain theory? *Nature Reviews Neuroscience*, 11(2), 127-138.
3. Sutton, R. S., & Barto, A. G. (2018). *Reinforcement Learning: An Introduction* (2nd ed.). MIT Press.
4. Maturana, H. R., & Varela, F. J. (1980). *Autopoiesis and cognition*. Springer.
