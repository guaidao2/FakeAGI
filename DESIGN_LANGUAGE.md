# DESIGN — 语言能力架构：符号接地语言器官（Symbol-Grounded Linguistic Organ）

> 状态：设计定稿（2026-08-01）
> 目标：在不违反十大哲学原理的前提下，为 FakeAGI 加入语言能力。
> 核心约束：无外挂奖励（语言信号必须内生）、生长即学习（词汇量可增长）、
> 与现有感知器官同构（语言 = 又一个模态器官）。

## 0. 核心决策

**语言不是一个独立模型，而是另一个感知器官（Linguistic Organ）**——
与视觉器官（P6 器官生成器）完全同构：token 流 → 嵌入原语 → 特征向量 →
接入同一个观测抽象层 → 世界模型学习"词 ↔ 状态"的映射。

语言的意义 = 预测收益（符号接地原理）：
- 一个词"有意义"，当且仅当听到它能让世界模型对未来状态的预测更准。
- 一句话"有意义"，当且仅当它减少了系统的不确定性（信息论定义）。
- 说话 = 选择使"听者"（世界模型模拟）预测误差最小的词序列。

这完全符合：
- ③ 智能 ≡ 预测误差最小化
- ④ 预测误差是唯一修正信号（语言通过预测收益接地，无外挂奖励）
- ⑧ 符号压缩是通信的产物（语言就是符号压缩）
- ⑨ 生长即学习（词汇表 = 嵌入维度，可增长）

## 1. 架构总览

```
                ┌──────────────────────────────────────┐
                │           认知核心（不变）             │
                │  LNN → 叠加态世界模型 → 决策委员会      │
                └──────────────┬───────────────────────┘
                               │ 状态向量 h（共享）
        ┌──────────────────────┼──────────────────────┐
        ▼                      ▼                      ▼
┌───────────────┐    ┌────────────────┐    ┌──────────────────┐
│ 视觉器官(P6)   │    │ 本体感觉器官     │    │ 语言器官(NEW)     │
│ 像素→特征      │    │ 运动/位置→特征  │    │ token→嵌入→特征   │
└───────────────┘    └────────────────┘    └──────────────────┘
                                                    │
                                     ┌──────────────┴──────────────┐
                                     ▼                             ▼
                            ┌─────────────────┐          ┌─────────────────┐
                            │ 理解路径         │          │ 说话路径         │
                            │ 词→状态预测      │          │ 状态→词选择      │
                            │ (世界模型条件化)  │          │ (最小描述长度)    │
                            └─────────────────┘          └─────────────────┘
```

## 2. 模块设计

### 2.1 语言器官（`cognition/language/grounding.py`）

```
class LinguisticOrgan(nn.Module):
    - token_embed: nn.Embedding(vocab_size, embed_dim)   # 词嵌入（可增长）
    - position_embed: 位置编码（token 顺序信息）
    - encoder: 小 GRU/线性层把 token 序列压成固定向量
    - 输出：language_vector（与观测抽象通道同维度）
    
    生长：vocab_size 增长 = Embedding 维度扩展（复用 P6 grow 逻辑）
```

### 2.2 理解路径（Grounding）

世界模型从 `predict(h, action)` 扩展为 `predict(h, action, language)`：
- 条件语言向量 → 世界模型预测下一状态
- 训练信号：无变化——预测误差（真实状态 vs 预测）
- **接地发生**：当"词 food 出现 + 食物方向状态"同时出现时，
  世界模型学会"food → 食物方向的预测修正" → 词获得预测价值

### 2.3 说话路径（Production）

```
select_words(state, vocab) = argmin_words 世界模型预测误差(state | words)
```
- 系统想"描述"当前状态 → 遍历候选词（或贪心）→ 选使世界模型
  从"无词预测"到"有词预测"误差下降最大的词
- 这就是最小描述长度（MDL）原则：说最有信息量的词
- 不需要单独的生成模型——复用世界模型（说话=逆接地）

### 2.4 词汇增长

- 词汇表 = 环境提供的 token 集合（先固定小词表：food/water/danger/safe/near/far...）
- 新词出现（环境词汇扩展）→ Embedding 加行（P6 式生长）
- 词义变化（环境语义漂移）→ 世界模型自动重接地（叠加态坍缩）

## 3. 接入点（最小侵入）

```
CognitionPipeline.process():
  1. obs（感知）→ 器官（P6）→ 抽象向量
  2. language_tokens（可选，config 开启）→ LinguisticOrgan → language_vector
  3. combined = [abstract_obs, language_vector, self_state]
  4. LNN → 世界模型（条件含 language）
  5. 说话：每 N tick，select_words(当前状态) → 输出词

config: "language": True/False（默认 False，低维环境零影响）
```

## 4. 验证实验（`test_language.py`）

场景：食物/水源/危险事件伴随语言标记（环境提供词）| 阶段 | 输入 | 验证目标 |
|------|------|---------|
| 1. 接地 | 状态 + 对应词 | 世界模型学会"词→状态"预测（理解） |
| 2. 纯词预测 | 只给词不给状态 | 系统从词推断状态（理解成立） |
| 3. 说话 | 只给状态 | 系统选词描述状态（说话成立，准确率） |

判定：
- 阶段2：词→状态预测误差 < 无词基线（证明词有预测价值=已接地）
- 阶段3：选词准确率 > 随机（证明能说话）

## 5. 为什么这是"最可能"的实现

1. **复用最大化**：EmbedPatch 原语、观测抽象层、世界模型条件预测、
   P6 器官生长——全部现成，语言只是又一个通道
2. **哲学自洽**：无外挂奖励（预测误差接地）、生长（词表可扩）、
   符号压缩（语言=压缩表示）
3. **可验证**：理解/说话都有量化指标，可判定通过/失败
4. **可扩展**：词汇量、语言复杂度、多语言逐步增长

## 6. 明确不做（当前阶段）

- ❌ 接外部 LLM（违反自包含哲学，且引入外挂知识）
- ❌ 语法树/句法分析（词袋级接地先行，句法后置）
- ❌ 长文本生成（先单次短句：状态→几个词）

## 7. 语言生存压力（P8a，已实现）

**核心命题：当语言成为生存的必要信息通道时，系统才会真正理解语义。**

- 环境：`test_language_survival.py` — 16×16 网格，食物方向对系统隐藏（观测只有水源方向），语言（"food east/west/north/south"）是唯一食物线索，能量只能从食物获得
- 机制：**可信度驱动的语言使用**（`main.py` `_language_trust`）
  - 强化：听词 → 找到食物 → trust +0.1
  - 坍缩：听词 → 未找到 → trust -0.0005（假线索缓慢杀死信任）
  - 试探：trust 归零 → 2% 概率重新听词（好奇心保底，防死锁）
- 结果：对照组（语言关）213 tick 饿死/0 食物 vs 实验组（语言开）916 tick/30.2 次食物——**语言对生存的价值 4.3 倍**
- 意义：语义不是被教会的，是被生存压力逼出来的——语言进入因果回路（词→方向→觅食→能量），预测收益驱动理解

## 8. 下一步

- P8c：句法/组合性（短语→简单句→因果句）

## 9. 语言意图（P8b，已实现）

**核心命题：系统主动用语言达成目的（L4 意图）。**

- 环境：`test_language_intent.py` — 环境不再主动提供信息，系统必须**说**才能获得方向（说 "food" → 环境回应食物方向词）
- 机制：
  - `AGI.speak()`：说话概率 = 需求强度 × 说话信任（饥饿→说 food；说话有用→信任升→更常说）
  - `意图优先仲裁`：主动请求的语言指令 +0.6 加成压过本能反射（生物对应：问路后按指示走）
  - 恐慌模式例外：危机时不听词（保命优先）
- 结果：会说 2000 tick 满存活/24.6 食物 vs 不会说 241 tick 饿死/0.4 食物
- 意图证据：饥饿时说 "food" 概率显著更高（低能-高能差 0.15，729 次说话）——说话由内部需求驱动，非固定节奏
- 意义：语言从"感知器官"（E9）→"生存依赖"（E10）→"执行工具"（P8b），完整闭环成立
