# AGI — 整体架构设计

**玄幕安全团队 - guidao2**

## 系统架构（三层耦合循环）

```
┌─────────────────────────────────────────────────────────────┐
│                       自我层 (Self)                         │
│  自模型 · 存在概率 · 好奇心预算 · 价值系统                   │
│  (持久存在，不随任务切换而改变)                               │
└──────────────────────────┬──────────────────────────────────┘
                           │ 存在信号驱动一切
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    认知层 (Cognition)                        │
│                                                             │
│  ┌──────────┐   ┌──────────┐   ┌────────────┐              │
│  │ 连续编码   │   │ 时序推理  │   │ 世界模型    │              │
│  │ CNN/感知   │◄─►│ LNN + τ  │◄─►│ 预测 + 惊奇 │              │
│  └─────┬────┘   └────┬─────┘   └──────┬─────┘              │
│        │              │                │                     │
│  ┌─────▼────┐   ┌────▼─────┐   ┌──────▼─────┐              │
│  │ 离散编码   │   │ 策略矩阵  │   │ 反事实通道  │              │
│  │ 符号/状态  │◄─►│ GameNN   │◄─►│ 想象+梦境   │              │
│  └──────────┘   └──────────┘   └────────────┘              │
└──────────────────────────┬──────────────────────────────────┘
                           │ 行动改变环境
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    物理层 (Physical)                         │
│  物质接口 · 行动执行 · 能量管理 · 感知输入                   │
│  (由具体环境实现的适配器)                                     │
└─────────────────────────────────────────────────────────────┘
```

## 模块设计（当前实际结构）

```
agi/
├── core/                    # 自我层 — 持久存在的"我"
│   ├── body.py              # 身体模型：6维稳态（能量/水分/结构/温度/疲劳/压力）+昼夜节律
│   ├── drives.py            # 驱动力系统：6驱动力（饥饿/口渴/恐惧/疲劳/好奇/无聊）竞争
│   ├── self_model.py        # 自模型：存在概率、状态历史、好奇心预算
│   ├── value_system.py      # 可进化价值系统：核心不可变 + 次级经验可调
│   ├── homeostasis.py       # 稳态维持：关键变量监控
│   ├── goals.py             # 目标层：目标vs过程分离（持久目标+落差+信息寻求）
│   ├── info_seeking.py      # 信息寻求：定向扫掠（有记忆系统性搜索）
│   ├── process_selector.py  # 过程选择：语言×目标层统一（可靠性估计+argmax）
│   ├── other_agent.py       # 他者模型：真他者跟踪（意图分类+竞争回避）
│   ├── emotion.py           # 情绪系统：显式情绪信号（恐惧/好奇/平静）+决策调制
│   ├── cross_domain.py      # 跨域迁移：底层能力提取+少样本微调（域抽象）
│   ├── transfer_selector.py # 迁移价值评估：何时迁移的元认知（多假设贝叶斯）
│   └── physics_intuition.py # 物理直觉：重力/碰撞/连续/动量/无瞬移先验（贝叶斯修正）
│
├── cognition/               # 认知层 — 感知+推理+决策
│   ├── __init__.py          # 认知管线：LNN+世界模型+GameNN+生长检测+误差通路
│   ├── temporal/            # 时序推理
│   │   ├── lnn.py           # LNN核心 (LTCell)，增量生长+稀疏突触+tau迁移
│   │   ├── tau_adapt.py     # τ自适应调度
│   │   ├── world_model.py   # 条件世界模型：hidden+action→next，感知/行动双通路
│   │   └── superposition_world.py  # 薛定谔叠加态世界模型：多分支预测+观测坍缩+分裂生长
│   │
│   ├── perception/          # P6 感知器官（可生长的"眼睛"）
│   │   ├── primitives.py    # 结构原语：Conv/Pool/Embed/Norm/Linear/Identity
│   │   ├── organ.py         # Organ：原语链 + fitness(误差下降量) + replicate/mutate/prune
│   │   └── generator.py     # OrganGenerator：超量生成+竞争选择+生存门控
│   │
│   ├── language/            # 语言器官（符号接地）
│   │   └── grounding.py     # LinguisticOrgan：token嵌入+编码器+word_probe 说话；SymbolGrounding：接地训练
│   │
│   ├── decision/            # 决策
│   │   ├── gamenn.py        # GameNN多策略博弈（Q网络+策略权重+TD学习）
│   │   └── committee.py     # 人脑式决策委员会：5决策者并行投票+加权仲裁
│   │
│   ├── metacognition/       # 第五层认知
│   │   ├── core.py          # 元认知层：缺口检测→目标生成→override
│   │   ├── __init__.py      # GapDetector（快速失败/世界模型/策略/因果缺口）
│   │   ├── goal_gen.py      # 探索目标生成器
│   │   ├── scheduler.py     # 好奇心调度器
│   │   ├── assess.py        # 自我评估
│   │   └── strategy_manager.py # 元-元认知：学习策略切换（default/explore/replay/grow/rest）
│   │
│   ├── planner.py           # 规划：BFS前瞻模拟（world model 滚动）
│   ├── imagination_channel.py # 反事实通道：真实经验+动作变异→想象经验
│   ├── concept_bank.py      # 概念库：概念提取+组合式反事实生成
│   ├── latent_state.py      # 隐变量模型：不可观测因素推断（固定4维上下文）
│   ├── attention.py         # 注意力门控：信息量+显著性+驱动力调节（保守identity策略）
│   ├── spatial_memory.py    # 空间记忆：增量地图节点+遗忘衰减+寻路
│   ├── sleep.py             # 睡眠循环：疲劳/能量条件+记忆巩固
│   ├── hemin.py             # 他者模型：影子自我对比
│   └── danger.py            # 危险感知
│
└── growth/                  # 生长引擎
    └── monitor.py           # 容量监控（loss/惊奇plateau检测）
```
## 核心循环（每 tick）

```
1. 感知 → 注意力门控（选择性感知）+ 物理先验检查（瞬移→应激）
2. LNN 时序更新 (τ自适应) → 状态向量 h_t
3. 世界模型条件预测 (hidden+action) → 惊奇 → 误差通路选择
4. 隐变量推断（持续高误差 → 不可观测因素）
5. 自模型更新：内部状态 + 存在概率
6. 驱动力更新 → 竞争主导
7. 认知处理 → GameNN 决策 + 规划前瞻
8. 人脑式决策委员会：反射/边缘/习惯/规划/元认知 5 路投票 → 加权仲裁
   （冲突→深思模式，危急→恐慌模式）
9. GameNN 在线学习（TD 误差 + 能量奖励）
10. 动作执行 → 环境改变 → 回到 1
11. 每 300 tick：概念库组合式反事实生成
12. 元-元认知：学习策略监控（误差无改善→切换策略）
13. 睡眠时：经验巩固 + 疲劳恢复
```

## 设计原则

| 原则 | 含义 |
|------|------|
| **自维持优先** | 所有模块服务于同一个目标：最大化存在概率 |
| **无外挂奖励** | 没有外部 reward 函数。所有信号由自模型生成 |
| **双层编码** | 信息流同时经过连续层和离散层 |
| **生长即学习** | 学习不只是调权重。学习也可以是长神经元 |
| **反事实必需** | 没有想象能力就没有因果推理 |
| **数据是资产** | 所有经验（含想象）都存储、巩固、复用 |
| **叠加态预测** | 世界模型维持多个可能未来分支（波函数），观测到达时坍缩；容量不足时分裂新分支——模型复杂度随世界复杂度生长 |

## 薛定谔叠加态世界模型

对应哲学底座之 ③ 智能 / ④ 误差修正 的量子化实现：

- **叠加（Superposition）**：对未来不预测单一确定值，而是同时维持 N 个可能分支（`SuperpositionBranch`，各自独立预测网络 + 振幅）。系统面对隐藏规则时能同时持有"吃→能量↑"与"吃→能量不变"等多个假设。
- **坍缩（Collapse）**：真实观测到达时，按各分支预测误差 softmax 更新振幅——匹配分支振幅上升，不匹配衰减。坍缩后残余熵即"量子化惊奇"：系统能感知自己有多不确定。
- **分裂（Split）**：当所有分支都持续坍缩失败（熵接近 ln(n) 理论上限），说明模型容量不足 → 从最强分支分裂出新分支（继承权重+变异），注册进生长协调器（`world_branches`）。复杂度随世界复杂度增长，永不"装不下"。
- **融合**：surprise = 预测误差 + 坍缩熵×0.3，两种不确定性来源相加保留全部信息。

验证：`test_superposition.py`（分支 3→5 分裂、熵 1.57→0.28 坍缩、t=52 发现隐藏规则）；A/B 对比（`test_ab_worldmodel.py`）显示叠加态学习速度比确定性世界模型快 39%（86.7→52.7 tick）。

## P6 感知器官生长

对应哲学底座之 ⑨ 生长/进化 的结构级实现（区别于广度扩维）：

- **原语库**（`cognition/perception/primitives.py`）：ConvPatch（局部卷积）、PoolPatch（降采样）、EmbedPatch（符号嵌入）、NormPatch（归一化）、LinearPatch（线性）、IdentityPatch（直通）——器官的"基因"。
- **Organ**（`organ.py`）：原语链 + fitness（预测误差下降量，A 度量防好奇漩涡）+ replicate（皮层柱复制堆叠）/ mutate（NEAT 参数变异）/ prune（突触修剪）。
- **OrganGenerator**（`generator.py`）：超量生成 3 候选 → 竞争期候选输出**真实替换观测流**（世界模型预测误差 = 候选"眼睛"的感知质量）→ fitness 最优保留、其余凋亡（神经营养假说）→ 生存门控（`body.integrity` 低时不生成）。
- **接入**：`len(obs) ≥ 16` 触发器官路径（低维直通零行为变化）；器官成熟后压缩观测（64D→8D）并重建观测抽象层；注册进生长协调器（`perception_organ`）。

验证：`test_organ_gen.py`（64D 像素流：t=0 超量生成 3 候选 → t=80 竞争保留 conv → t=36 找到隐藏食物，6/6 通过）。

## 语言器官（符号接地）

对应哲学底座之 ⑧ 符号压缩 的实现。设计文档：`DESIGN_LANGUAGE.md`。

- **核心原理**：语言 = 又一个感知器官。词的意义 = 预测收益——一个词"有意义"当且仅当听到它能降低世界模型预测误差。
- **理解**（`cognition/language/grounding.py`）：`LinguisticOrgan`（token 嵌入 + 位置编码 + 编码器）把词序列压成语言向量，拼入观测；`train_grounding` 用"词→状态方向"回归做真实梯度接地训练。
- **说话**：`word_probe`（LNN hidden → 词偏好）——系统用内部状态描述世界（最小描述长度原则，非词循环）。
- **词汇生长**：`grow_vocab` 扩展 Embedding + word_probe 输出（保留旧权重与输入维度）。
- **接入**：`config.language=True` 开启（默认关闭，低维环境零影响）。

验证：`test_language.py`（阶段1 接地训练 loss=0.016 → 阶段2 理解：词→状态误差 0.405 vs 随机基线 0.525 → 阶段3 说话：纯状态→词准确率 40%，3/3 通过）。

**语言生存压力（P8a）**：语言进入生存回路——`test_language_survival.py` 中食物方向对系统隐藏，语言是唯一线索。对照组（语言关）213 tick 饿死/0 食物，实验组（语言开）916 tick/30.2 次食物。机制：可信度驱动（`_language_trust` 强化/坍缩/试探），证明**当语言成为生存必要通道时系统才真正理解语义**。

**语言意图（P8b）**：语言从感知升级为执行器——`test_language_intent.py` 中系统必须**主动说话**（说 "food" → 环境回应方向）才能找食。`AGI.speak()` 需求驱动（概率=需求×信任），`意图优先仲裁`让主动请求的指令（+0.6 加成）压过反射（问路后按指示走）。会说满存活/24.6 食物 vs 不会说 241 tick 饿死；饥饿时说 food 概率显著更高（差 0.15）——L4 意图成立。

## 目标层与信息寻求（E13）

对应哲学底座之 ③ 智能 / ④ 误差修正 的"目标 vs 过程分离"实现。设计文档：`DESIGN_GOALS.md`。

- **目标层**（`core/goals.py`）：`GoalState`/`Goal`——持久目标表征（能量/水维持，由身体结构内生，不随观测消失）+ 落差 = 目标态 − 当前态（显式驱动信号）。
- **信息寻求**（`core/info_seeking.py`）：`InfoSeeker`——定向扫掠（已访问标记 + 未探索方向优先 + 系统性覆盖），落差门控启动、正回报停止。**有记忆的系统性搜索，非随机探索**。
- **接入**：`_info_seek_enabled` 显式门控（默认关闭，旧实验零影响）；扫掠覆盖委员会动作（panic 例外保命优先）。
- **验证**：`test_goal_layer.py`（7 组消融，n=3 弱支持）——① G6 定向扫掠 3.7 食物 vs G5 恒定探索 0.7（5 倍）② G6 vs G6b 扫掠恒开 2.7（落差门控 1.4 倍增量）。公理④ 精炼（落差为主信号、激活以目标先验为前提）获配置有效性证据。

## 过程选择（②：语言 × 目标层统一）

设计文档：`DESIGN_PROCESS_SELECTION.md`（约束经三轮外部讨论加固）。

- **核心**：将"说话"重构为信息寻求的一种过程选择——系统在"问路"与"自己扫掠"之间，由在线可靠性估计驱动的 argmax 选择（目标不变、过程可换）。
- **实现**（`core/process_selector.py`）：`ProcessEstimator`（可靠性在线估计：成功 +0.1/失败 -0.15，frozen 可冻结）+ `ProcessSelector`（argmax 预期收益 + 5% 试探防死锁）+ 真机会成本（问路 tick 不移动，intrinsic）。
- **验收**（`test_process_selector.py`）：V1 失败→可靠性↓（0.5→0.05）/ V2 误差进模型 / V3 可逆（经试探）/ V4 可靠性驱动切换；N1-N4 负对照（N1 冻结仍问 200±0 vs N4 160±3、N2 全败少问 13±3 vs N3 179±35）。
- **完整循环接入**（`test_process_integration.py` + `run_process_selection_study.py`）：冒烟 6 项全过；n=30 预注册判定全过（H1：N2 2±0 vs N3 390±149 标量版 / 209±156 叠加态版；N1：fail 环境冻结 125 vs 在线 2——在线更新必要；零影响护栏 ask=0）。
- **C2 叠加态升级**（`SuperpositionEstimator`，`test_c2_superposition.py`）：多假设分支 + 贝叶斯坍缩 + 置信度地板——被教歪可逆（0.11→0.89 回升且低假设保留），4 项验证 + n=30 回归全过。
- **边界诚实**：reliability 为答对率代理（叠加态已实现，落差消解率精确化待做）；n=30 预注册。

## 他者模型（⑦ 自模型前置：社会智能）

- **实现**（`core/other_agent.py`）：`OtherAgent`（competitor/cooperator/wanderer 策略）+ `OtherModel`（位置记忆 + 冲突计数 + 意图分类 + 竞争回避）。
- **验证**（`test_other_agent.py`，5 项全过）：意图识别（竞争 10/10、合作 9/10）、竞争回避（距离增益 +0.8）、合作不回避（+0.0）、资源分配（竞争被抢 8.3 vs 合作 193.8）。
- **与 hemin 区分**：hemin 是反事实影子（自己→不同动作），此为真实独立实体跟踪。

## 情绪系统（情感是物理的）

- **实现**（`core/emotion.py`）：生理（能量/健康/应激/危险）+ 认知（surprise）→ [fear, curiosity, calm] 情绪向量 + `modulate_action`（恐惧→激进探索）。
- **验证**（`test_emotion.py`，5 项全过）：快饿死→fear 0.72、高应激→1.0、高 surprise→curiosity 0.73、稳态→calm 0.85、恐惧调制探索率 0.54 vs 0.12。
- **主循环接入**：`_emotion_enabled` 开关（默认关），surprise 后调制探索率（接入验证 4 项全过）。

## 句法层（P8c：语言词级→句级）

- **实现**（`cognition/language/syntax.py`）：`BigramTracker`（相邻词对频率）+ `PhraseBuilder`（按经验顺序组词）+ `integrate_with_select`（select_words 多词 + 句法排序）。
- **可学习语法**：词序从经验统计来，经验翻转输出跟着翻（非硬编码规则）。
- **验证**（`test_syntax.py`，4 项全过）：词序学习 0.83、短语 [food,east]、顺序反转、select_words 集成。

## 跨域迁移（⑨ 生长/进化的路径）

- **④ 特征层迁移**（`core/cross_domain.py`）：`DomainAdapter` + `CapabilityExtractor` + `CrossDomainTransfer`（底层注入 + 全模型微调 + 相似度）。**诚实负结果**：初版 7 倍优势为堆牌已废弃；重做后迁移不占优（0.97x/0.86x）——骨架保留。
- **⑤ 策略层迁移（触类旁通）**（`test_transfer_analogy.py`）：关系同构域对（迷宫墙→威胁场，共享 8D 关系特征空间）。**少样本迁移成立（37 vs 20）**；零样本不成立（初版"17vs0"为停留基线伪胜利，复审修正——关系语义需真正同构：墙=不可走 vs 威胁=可走不同则失败）。结论：迁移起点复用有效，关系映射是触类旁通真正门槛。
- **⑥ 迁移价值评估**（`core/transfer_selector.py` + `test_transfer_selector.py`）：何时迁移的元认知——多假设贝叶斯 + 幅度加权（平局=迁移无优势→降）+ 保守阈值决策（0.60）。同构经验升可靠性（0.69→选迁移）、异构经验降（0.53→选从头），有评估器 21.5 vs 无脑迁移 18.5（5 项全过）。**已接入主循环**（`set_env` 环境切换决策：迁移保留/从头重置 GameNN，默认关闭，接入验证 5 项全过）。
- **全模块协同（E14）**（`test_emergent.py`）：语言+情绪+他者全开迷宫世界 10000 tick——5 项全过（多模块激活 3/5、情绪 467 次、语言 466 次影响决策、多 seeds 全模块 0.6±1.2 vs 对照 0.0±0.0——种子修复后重跑，结论稳定：全模块始终≥对照）；暴露睡眠未触发集成短板（单模块测试发现不了）。
- **概念迁移（路线 B）**（`test_concept_transfer.py`）：跨表面抽象"可消耗物"——训练 food+water 两种表面，零样本测 fruit 第三种表面——3 独立 seeds 全 50/50 vs 随机 19-23（100% 迁移）；结论诚实降级"跨表面泛化（机制未明）"因外观权重 0.358 未收敛；v1 教训（Q 表查表无法测概念/方向混叠）如实记录。

## 哲学底座（决定架构选择的十条原理）

### 依赖层级
```
① 物质-信息（地基，不可攻击否则全部坍塌）  ← 本图为"哲学原理"体系
 └→ ② 活着（系统边界）                    （实验编号 ⑤⑥ 是迁移系列 T2/T3，
     └→ ③ 智能（优化目标）                 见下方"跨域迁移"章节——两者独立）
         ├→ ④ 误差修正 ──┐
         ├→ ⑤ 时间约束 ──┼→ 学习闭环（⑤⑥产生误差 → ④消费误差）
         └→ ⑥ 耦合循环 ──┘
             └→ ⑦ 自模型（元层）
                 ├→ ⑧ 符号压缩（表示）
                 └→ ⑨ 生长/进化（路径）
                     └→ ⑩ 价值注入（初始化，叶子节点）

工程映射：
 ① → 硬件/计算资源
 ② → BodyModel
 ③ → 自模型优化目标
 ④ → 学习算法/置信度门控
 ⑤ → LNN τ机制
 ⑥ → 主循环（tick loop）
 ⑦ → SelfModel 模块
 ⑧ → Encoder + GameNN 离散策略
 ⑨ → Growth Engine
 ⑩ → config.yaml / 初始化参数
```

### 原理展开

**① 物质是基底，信息是描述。** 信息不能脱离物质存在，物质不需要信息来存在。关系是不对称依赖，不是对偶。工程哲学立场：这不是本体论声明，是工程选择——任何不依赖物质的智能系统目前没有已知的实现路径。如果未来证明信息可以脱离物质独立存在，本条需要修正。

**② 活着是在模型层面维持自身结构的稳定性。**
```
弱（恒温器就够）：稳态 / Cannon
中（化学网络就够）：自创生 / Varela
强（工程选择，需要预测+行动）：自由能原理 / Friston ← 你的系统需要这一层
判据：Ashby 必要多样性定律 — 内部多样性 ≥ 外部多样性
```
系统和环境在本体论上是共同演化的（③中"互相定义"），但在工程实现上必须有固定的接口定义——tick循环的输入输出构成系统边界。

**③ 智能是为维持存在而发展出来的工具集。** 维持存在 ≠ 解决问题。问题解决是工具，不是目标。控制论角度：系统不是环境的函数，环境也是系统的函数。情感/动机是预测误差的加权机制——不是独立模块，而是决定"哪些预测误差值得关注"的选择器。没有情感参与的纯理性决策无法做出选择（Damasio 躯体标记假说）。

**④ 预测误差是基础的修正信号。修正有边界。** 所有修正信号最终都必须通过预测误差机制消费，但信号的来源可以多样——演化中的选择压力、外部奖励、价值函数的偏差，最终都表现为预测误差。单次误差的修正幅度 ≤ 模型置信度的函数。持续误差 → 结构变更（生长/剪枝），而非参数调整。预测误差有两条响应通路：感知通路（误差 → 更新内部模型）和行动通路（误差 → 改变环境使其符合预测）。选择哪条通路取决于"误差是否可通过行动消除"——不可行动的误差（如过去已发生的事）只能走感知通路。子条款：遗忘是预测结构的主动维护。修正信号的有效性随时间衰减，历史噪声不会无限积累。不是信息丢失，是信噪比维护。

**⑤ 时间是信息的不可逆维度。** 热力学：熵增方向 = 时间箭头。信息论：过去确定，未来只有概率分布。时间不是外部框架，是系统内部的预测结构。工程：LNN τ机制是这个结构的实现。子条款：记忆的有效性随时间衰减，这是主动的信噪比维护，不是信息丢失。

**⑥ 认知是感知-决策的耦合循环（Active Inference，工程选择）。** 系统通过行动使环境变得可预测，而非被动等待输入。耦合循环意味着：观测者的行动改变了被观测系统的状态，这是物理事实，不是哲学立场。

**⑦ 意识 = 自模型（工程选择，非本体论声明）。** 选择 Metzinger 自模型框架作为实现路径，因为它是当前唯一可以直接写成模块的意识理论。前置条件：自模型的训练信号之一来自"他者模型"。系统先建模"另一个agent在做什么"，再通过对比产生"我在做什么"。

**⑧ 符号压缩是任何智能系统的必需结构。** 连续层（LNN/向量）→ 快速适应。离散层（GameNN策略索引）→ 稳定推理。在当前架构下，纯连续 LNN 能解迷宫但做抽象推理的效率远低于有离散层的系统。这不是理论上的"不能"，而是效率/泛化的差距。

**⑨ 通用智能不需要奇迹，只需要正确的架构和足够的时间。** 但"足够的时间"不是无限——架构搜索本身也需要结构化引导，不能是盲目搜索。阶段三的标志不是"加反事实模块"，而是"高层模块能修改低层模块的拓扑结构（增删节点/连接）"。结构变更的约束：任何拓扑修改必须保持自模型的核心变量（存在概率、身份向量）连续。如果一次修改导致自模型预测误差超过阈值，该修改被回滚。类比：大脑神经可塑性有边界——你不会一觉醒来变成另一个人。

**⑩ 价值由架构师在初始化时注入，但分为两个层级：不可变约束和可调参数。** 不可变约束是架构级的，任何内部修改不能触碰——包括"存续优先"（冲突时系统优先执行存续判断）和"架构师不能注入要求系统自我终止的指令"。可调参数（好奇心权重、探索率初始值）系统可在运行中自适应调整。系统的任务不是"发现价值"，而是"在给定价值下最大化存续"。

### 可证伪性
- 若存在一个无自模型、无他者模型、仅靠任务优化的系统，表现出跨域迁移和自主目标设定 → ②③⑦错
- 若纯连续表示（无离散符号层）能高效完成组合泛化推理 → ⑧错
- 若冻结 World Model + 固定策略的系统在 ≥ 50 类未见过的环境扰动后仍无需人工干预即可存活 → ④⑤⑥错
