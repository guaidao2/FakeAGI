# 规则变化适应中的多通道误差修正：FakeAGI 与 Active Inference 基线的对比研究

**FakeAGI 分论文一**（总论文之外的独立实证论文）
总论文（理论基座）：[PAPER.md](PAPER.md)（十一公理体系与架构全貌）

玄幕安全团队 - guaidao2 · 墨渊安全实验室 - coolmoon · 2026-08

---

## 摘要

主动推理（Active Inference）框架下的智能体（以 pymdp 为代表）通过单一生成模型参数（Dirichlet A 矩阵）的在线更新适应环境规则变化。本文提出并实证对比另一类架构——**FakeAGI**——其误差修正分布在**多通道**（世界模型预测、概念库价值锚、GameNN 时间差分学习），并附加**概念层符号化**与**生理驱动耦合**。在 5×5 网格资源环境中引入规则变化（食物位置在 tick 300 迁移），对比结果显示：剥削（exploitation）阶段 pymdp 以精确贝叶斯推断占优（295 次食物获取 vs 110）；但在规则变化后的适应阶段，FakeAGI 的适应能力经接线审计（B1-B3 死通路修复——原 3/5 部分依赖伪 boredom）后如实表述为**长时窗性质**：2000 tick 下 3/3 恢复（52-62 次），短窗 600 tick 2/5（27/8——种子路径敏感：变化时位置/能量决定 300-600 tick 能否到达新食物；任务一修复：反射阈值 0.95 利用方向信息 + 到达停留本能）；pymdp 在全部种子上未恢复（0 次）。抗辩实验（lr 扫描 0.1-1.0、只学 A、长时窗 2000 tick）确认：学习率非 pymdp 不适应的原因；**代谢对称化复核**（原 FakeAGI 端代谢 10× 慢——已对称）曾显示长时窗 FakeAGI 也死于稳态失衡（546-762 tick——口渴驱动力不足）——**根因定位与修复（欲望架构阶段 A）**：`reflex_vote` 方向恒为食物、`get_action_bias` 无 thirst 偏置——修复后长时窗 FakeAGI 3/3 活满 2000 并 3/3 适应（52/62/55）vs pymdp 499 死 + 0 适应——**"动态稳态 vs 静态偏好"第二增量以真机制成立**。本文给出该差异的数学分析（多通道误差信号的收敛优势与单通道 Dirichlet 更新的先验强度惰性 + §3.4 水动力学稳态），并报告概念层零样本迁移（80%+）作为补充证据。结果表明：**多通道误差架构 + 生理驱动耦合 + 概念符号化是主动推理在规则变化场景下的有效补充，而非替代**。

**关键词**：主动推理；自由能原理；规则变化适应；概念符号化；多通道误差修正

---

## 1. 引言

### 1.1 背景

自由能原理（Free Energy Principle, FEP, Friston 2010）主张：自组织系统通过最小化变分自由能维持其存在。其工程化实现——主动推理（Active Inference）——通过离散 POMDP 生成模型（似然矩阵 A、转移矩阵 B、先验偏好 C）的变分推断选择行动，并在观测后在线更新生成模型参数（Dirichlet 计数）。该路线在理论完备性上领先，但工程实证多停留在玩具环境。

FakeAGI 是一套独立发展的构造性认知架构：由十一条哲学公理（物质-信息 → 能量-时间-精度三角权衡）推导，实现"活着→预测误差→概念→语言→社会"的逐层涌现。**本文不主张替代主动推理，而是定位差异**：在规则变化场景下，多通道误差修正 + 概念符号化是否提供主动推理缺失的适应能力。

### 1.2 贡献

1. 同环境（5×5 网格资源场 + 规则变化）双系统对比，量化剥削/适应差异；
2. 数学分析：多通道误差信号 vs 单通道 Dirichlet 更新的收敛差异；
3. 概念层零样本迁移作为补充证据；
4. 公理体系（④ 精度加权、⑨ 结构可塑性）与实验结果的对应。

---

## 2. 系统概述

### 2.1 十一公理（简述）

FakeAGI 的架构由十一条公理推导（详见总论文 §2）。与本文相关的三条：

- **公理④（误差修正）**：唯一的训练信号是预测误差；误差是精度加权的；存在三条响应通路（感知/行动/主动采样）。
- **公理⑧（符号压缩）**：离散符号是通信产物，概念层将连续观测压缩为可重用簇。
- **公理⑨（结构可塑性）**：固定容量失效——生长与修剪双引擎（当前实现含生长，修剪待实现）。

### 2.2 架构组件（本文涉及）

```
感知：连续方向向量观测（食物/水方向）
时间：LNN（液体时间常数网络——状态动力学 τ）
预测：条件世界模型（predict(h, action) → h'）
决策：GameNN（多策略 Q 网络 + TD 学习）+ 决策委员会（加权仲裁）
概念：ConceptBank（价值锚聚类——观测簇×价值绑定×符号化）
生理：BodyModel（能量/水稳态）+ 6 驱动力
```

### 2.3 pymdp 基线

pymdp（inferactively-pymdp 1.0.3）为主动推理的标准实现：A 矩阵编码感知似然（食物初始位置先验）、B 矩阵编码确定性转移、C 矩阵编码静态偏好（食物/水）、infer_parameters 执行 Dirichlet 在线更新。

---

## 3. 数学推导

### 3.1 统一视角：两者均为预测误差最小化

**主动推理**：最小化变分自由能

$$F = D_{KL}[q(s) \| p(s)] + \mathbb{E}_q[-\ln p(o|s)]$$

其中第一项为复杂度，第二项为似然误差。行动选择最小化期望自由能（预期精度 + 信息增益）。

**FakeAGI**：世界模型损失为一步预测误差

$$\mathcal{L}_{WM} = \| \hat{h}_{t+1} - h_{t+1} \|^2$$

其中 $\hat{h}_{t+1} = f_\theta(h_t, a_t)$。GameNN 为时间差分误差

$$\mathcal{L}_{TD} = (r_t + \gamma \max_a Q(s_{t+1}, a) - Q(s_t, a_t))^2$$

概念库为聚类距离（价值锚）。

**同构性**：三者均为"预测-实际差异"的度量——FakeAGI 的预测误差是 FEP 自由能的工程化（未做变分形式化）。**这是本文的坐标系声明**：我们不是在发明新学习信号，而是在同一信号上分布到多通道。

### 3.2 关键差异：误差通道数量与更新动力学

**单通道（pymdp）**：生成模型参数 $\theta = (A, B)$ 通过 Dirichlet 计数更新：

$$A^{(t+1)}_{ij} \propto A^{(t)}_{ij} + \eta \cdot \mathbb{1}[o_t = i] q(s_t = j)$$

有效学习率受**先验强度**控制：$A$ 初始化为确定感知模型（食物在 [0,0]），则规则变化后每个反例观测对 $A$ 的冲击为

$$\Delta A \sim \frac{1}{n_0 + n_{obs}}$$

其中 $n_0$ 为先验等效观测数。当 $n_0$ 大（强先验）时，**适应速度与 $n_0$ 成反比**——强先验的智能体在规则变化时适应慢。pymdp 的 $A$ 矩阵是确定性先验（$n_0 \to \infty$ 等效），导致 300 tick 内 $A$ 几乎不动——**先验强度惰性**。

**多通道（FakeAGI）**：误差分布到三个独立通道：

1. 世界模型：$\mathcal{L}_{WM}$ 梯度下降（学习率 $\alpha_{WM}$）——在线更新，无先验强度惰性（权重初始化均匀）；
2. 概念库：价值锚聚类——新观测直接形成新簇（$\mathrm{dist}(o, C) > \theta \Rightarrow$ 新簇）——**无参数更新，结构直接扩展**；
3. GameNN：TD 学习（学习率 $\alpha_{TD}$）——奖励信号直接修正 Q 值。

**收敛速度分析**：设规则变化发生在 $t_0$，单通道系统恢复到 $t_0$ 前性能所需时间 $T_{single}$ 满足

$$T_{single} \propto n_0 / \eta$$

（先验强度/学习率）。多通道系统的适应由**最快通道**主导：

$$T_{multi} \approx \min(T_{WM}, T_{concept}, T_{TD})$$

其中概念通道 $T_{concept}$ 为**常数级**（新簇即时形成——$\mathcal{O}(1)$ 观测即可），不受先验惰性影响。因此当 $n_0 / \eta \gg 1$（强先验）时，$T_{multi} \ll T_{single}$——**多通道的结构扩展通道规避了参数更新的先验惰性**。

**预测**：规则变化后 FakeAGI 的适应由概念通道（新簇）+ 世界模型（在线梯度）主导，应显著快于 pymdp 的 Dirichlet 更新。

### 3.3 概念符号化的泛化优势（补充推导）

设概念层将观测空间 $\mathcal{O}$ 压缩为符号空间 $\mathcal{S}$（映射 $g: \mathcal{O} \to \mathcal{S}$）。跨域迁移时，若域 A、B 共享符号结构（如"可消耗物"概念），则 B 域的学习初始先验为

$$p_B(s) = p_A(s | \text{共享概念})$$

零样本迁移成立当且仅当共享概念的判别力跨域保持：$\forall o \in \mathcal{O}_B,\ g(o)$ 的簇归属与域 A 一致。**概念层零样本迁移 80%+（实验 2）为该推导的实证**。

---

### 3.4 动态稳态：生理驱动的水动力学（第二增量——口渴导航修复后）

长时窗存活是稳态维持问题。设水储量 $W(t)$，代谢消耗率 $\mu_w$，喝水事件率 $\lambda(t)$，每次喝水补给 $\Delta W$：

$$\frac{dW}{dt} = -\mu_w + \lambda(t) \cdot \Delta W$$

**静态偏好（pymdp）**：$\lambda = \lambda_0$（固定 C 偏好 → 固定去水频率，与环境状态无关）。稳态条件 $\mu_w = \lambda_0 \Delta W$ 一旦不满足（代谢高于偏好补给率），水线性耗尽：

$$T_{death} = \frac{W_0}{\mu_w - \lambda_0 \Delta W} \quad (\lambda_0 \Delta W < \mu_w)$$

**动态驱动（FakeAGI）**：口渴驱动使 $\lambda(t) = f(W(t))$ 随储量递减而递增（W 低 → 口渴高 → 去水频率高）。稳态条件变为

$$\exists W^*: \quad \mu_w = f(W^*) \cdot \Delta W$$

只要驱动函数的可达区间覆盖代谢率（$f$ 峰值补给率 $\geq \mu_w$），稳态必然存在——死亡时间 $T_{death}$ 不封顶（长期维持）。**这是"活着"公理（②）的数学形态**：生理欲望 = 负反馈控制器，将储量拉回稳态。

**实证对照**：pymdp $T_{death} = 499$（$W_0=1.0,\ \mu_w=0.002$，$\lambda_0 \Delta W < \mu_w$——静态偏好补给不足）；FakeAGI 口渴导航修复后 2000 tick 活满（驱动函数覆盖代谢）——与模型预测一致。修复前 FakeAGI 死亡（546-762）恰因 $\lambda(t)$ 为常数（反射不朝水）——**同模型解释修复前后差异**：缺驱动反馈 = 退化为静态偏好。

（能量动力学对称：$\frac{dE}{dt} = -\mu_e + \lambda_e(t)\Delta E$——饥饿驱动同理，实验数据显示食物获取不封顶。）


### 4.1 实验 1：规则变化适应（FakeAGI vs pymdp）

**环境**：5×5 网格；食物 [0,0]、水 [4,0]；能量/水随时间衰减；tick 300 时食物迁移至 [4,4]（规则变化）。吃判定：邻近 <3 格 + 停留动作（两系统公平）。

**配置**：

| 参数 | FakeAGI | pymdp |
|------|---------|-------|
| 观测 | 连续方向向量 | 离散方向/类型（A 先验编码初始食物位置）|
| 动作 | 5（委员会仲裁）| 5（变分策略选择）|
| 偏好 | 动态 V（驱动耦合）| 静态 C（食物/水）|
| 学习 | 多通道 | infer_parameters（lr=0.5）|
| 判据 | 存活 tick / 变化前食物 / 变化后食物 | 同左 |

**结果（5 seeds 独立）**：

| seed | FakeAGI 变化前/后 | pymdp 变化前/后 |
|------|------------------|-----------------|
| 0 | 191 / 270 | 295 / 0 |
| 1 | 155 / 70 | 295 / 0 |
| 2 | 233 / 216 | 295 / 0 |
| 3 | 123 / 230 | 295 / 0 |
| 4 | 134 / 270 | 295 / 0 |
（对称代谢版本——2026-08 复核；早期慢代谢版数据作废）|

**解读**：
- 剥削阶段 pymdp 显著占优（295 vs ~110）——精确贝叶斯推断（与 §3.1 同构性一致：其单通道更集中）；
- 适应阶段 FakeAGI 长时窗（2000 tick）3/3 恢复（76-74 次），pymdp 0/3（499 tick 死）——支持 §3.2 预测（多通道规避先验惰性）；**接线审计后短窗 600 tick 适应 1/5（原 3/5 部分依赖伪 boredom——诚实降级，见 §4.4）**；第二增量（动态稳态 vs 静态偏好，§3.4 水动力学推导 + 口渴导航修复后）
- FakeAGI 短窗适应不稳定（1/5 seed 恢复）——探索-饥饿死循环需更长时间打破（长时窗 3/3 恢复——诚实记录）。

### 4.2 实验 2：概念零样本迁移（补充证据）

**设计**：迷宫食物域 → 威胁场资源域 → 村庄果实域，共享"可消耗物"概念；域 A 训练后零样本测域 B/C。

**结果**：零样本迁移 80%+（路线 B 判定 OK）——支持 §3.3 推导（共享符号结构跨域保持判别力）。

### 4.3 实验 3：多通道 vs 单通道消融（机制归因）

**设计**：关闭 FakeAGI 概念通道（concept_drive 关）对比全通道；关闭 GameNN 通道对比。

**结果**（诚实）：全通道组合不劣于对照（emergent_ablate：组合 3.0 vs base 1.0 历史记录；当前跑批"无显著拖累"）——**单通道消融未显著降低性能，机制归因需更精细配置**（如实记录）。

---

### 4.4 抗辩实验（lr 扫描 / 只学 A / 长时窗——2026-08 补充）

针对"lr=0.5 是否不公平"与"600 tick 是否太短"的审稿预期，补三项实验：

**lr 扫描（0.1/0.5/1.0 × 3seeds）**：变化后食物全部 0.0——**学习率不是 pymdp 不适应的原因**（lr 提升 2-10 倍无改善）——适应缺口源于机制（A 更新路径/先验强度惰性），非调参可解。

**只学 A（排除 B 干扰）**：变化后食物 0.0——排除 A+B 同时更新的干扰假设。

**长时窗（2000 tick × 3seeds——接线审计 B1-B3 修复后复核）**：

| 系统 | 存活 | 变化后食物 |
|------|------|-----------|
| FakeAGI | 3/3 活满 2000 | 52 / 62 / 55（3/3 适应）|
| pymdp | 3/3 死 499 | 0 |

**第二增量证据（长时窗——接线审计后确认更真）**：代谢对称化复核曾撤销"动态稳态"主张（FakeAGI 也死 546-762）。**根因定位与修复（欲望架构阶段 A）**：`reflex_vote` 方向恒为食物（obs[0:2]），口渴仅作触发条件——水方向从未用于导航；`get_action_bias` 完全缺失 thirst 偏置。修复后长时窗 FakeAGI 3/3 活满 2000 并 3/3 适应（52/62/55）vs pymdp 499 死 + 0 适应——**"动态稳态 vs 静态偏好"第二增量成立**。后续接线审计（B1-B3 死通路修复：survival_prob/boredom 数据真实化）后复核：长时窗结论**保持不变且更真**（探索由行为重复驱动——非伪 boredom）。

**第一增量诚实降级与部分恢复（接线审计 B1-B3 + 任务一修复）**：短窗（600 tick）适应经伪 boredom 暴露后（3/5→1/5）通过**任务一真实机制三修**恢复至 2/5（27/8）：① 反射阈值 0.8→0.95（全可观察不浪费方向信息——原阈值下变化后 175 tick 才触发反射）② 反射到达目标即停留本能（无停留票→走到不吃又走开）③ 信息寻求仅无线索时覆盖（盲扫覆盖反射）。**残余种子敏感性**：seed0/1/3 变化后 0——变化时位置/能量路径决定 300-600 tick 能否到达新食物。**诚实结论：适应增量是长时窗性质（2000 tick 3/3 稳定 52-62），短窗受种子路径限制——伪 boredom 夸大已清除，任务一机制为真实修复。**

**第二增量证据（长时窗暴露——代谢对称化复核后修正）**：原版（FakeAGI 代谢 10× 慢）长时窗 FakeAGI 活满 2000 vs pymdp 499 死——归因"动态稳态 vs 静态偏好"。**代谢对称化后重跑：FakeAGI 也死于稳态失衡（546-762 tick——口渴驱动力不足），仅存活略优于 pymdp（546-762 vs 499）——"动态稳态"主张撤销，降级为"存活略优"（机制：FakeAGI 去水频率仍高于 pymdp 静态偏好，但不足以长期维持——口渴驱动力实现弱）。**

**诚实边界**：pymdp 的 A 更新为何在 1000+ tick 后仍不生效——机制层面未完全定位（infer_parameters 更新路径或先验强度）；lr=2.0 超时未完成（不影响主结论：0.1-1.0 覆盖 10 倍范围）。

## 5. 讨论

### 5.1 差异定位（本文核心结论）

1. **剥削 vs 适应是两种能力**：pymdp 在已知环境更高效（集中单通道），FakeAGI 在规则变化下更鲁棒（多通道冗余）——**两者互补，非替代**；
2. **先验强度惰性是单通道主动推理的固有代价**：强感知先验（正确的 A 矩阵）在规则变化时成为适应障碍——精度加权（公理④认识论子条款）可缓解（低置信先验快速放弃）；
3. **概念符号化提供结构级适应**：新簇即时形成（O(1)），不受参数更新惰性影响——这是主动推理（纯参数更新）缺失的机制。

### 5.2 局限（诚实）

- 环境规模（5×5）与 tick 数（600）有限——适应差异需更长时窗（2000+）确认；
- FakeAGI 先验注入（反射/概念初始化）与 pymdp A/B 矩阵先验不完全对等——差异归因需对称配置复核；
- 消融未显示单通道显著贡献——多通道优势的机制归因待精细实验；
- pymdp 学习率（lr=0.5）敏感性未探索——更高 lr 可能改善其适应（待后续）。

### 5.3 与公理体系的关系

- 公理④（多通道误差 + 精度加权）——实证：多通道适应优势；精度加权为下一步实现（认识论子条款）；
- 公理⑧（符号压缩）——实证：概念零样本迁移 80%+；
- 公理⑨（结构可塑性）——概念新簇=结构级适应（参数更新之外的路径）。

---

## 6. 结论

本文在主动推理坐标系内定位了 FakeAGI 的两个实证增量：**① 规则变化适应（长时窗性质）——多通道误差修正 + 概念符号化规避了单通道 Dirichlet 更新的先验强度惰性**（2000 tick 3/3 vs pymdp 0/3；短窗 600 tick 2/5——伪 boredom 清除后任务一修复：阈值利用信息+到达停留，残余种子敏感性）；**② 长时窗动态稳态——生理驱动耦合（口渴导航）维持长期生存并适应（2000 tick 3/3 活满 + 52/62/55 次 vs pymdp 499 死 + 0 适应）**，同时诚实报告剥削阶段的差距（pymdp 295 vs FakeAGI 均值 ~167）。该结果支持"主动推理 + 构造性认知架构"的互补路线，并为公理④（精度加权）、公理⑨（结构可塑性）与公理②（活着）的实现提供实证动机。

---

## 参考文献

1. Friston, K. (2010). The free-energy principle: a unified brain theory? *Nature Reviews Neuroscience*, 11(2), 127-138.
2. Heins, C., et al. (2023). pymdp: A Python library for active inference in discrete state spaces. *Journal of Open Source Software*.
3. Oudeyer, P.-Y., & Kaplan, F. (2007). What is intrinsic motivation? A typology of computational approaches. *Frontiers in Neurorobotics*, 1, 6.
4. Harnad, S. (1990). The symbol grounding problem. *Physica D*, 42(1-3), 335-346.
5. Maturana, H. R., & Varela, F. J. (1980). *Autopoiesis and cognition*. Springer.
6. Sutton, R. S., & Barto, A. G. (2018). *Reinforcement Learning: An Introduction* (2nd ed.). MIT Press.
7. Han, S., et al. (2015). Learning both weights and connections for efficient neural networks. *NeurIPS*.
8. Chen, T., et al. (2020). A simple framework for contrastive learning of visual representations. *ICML*.
