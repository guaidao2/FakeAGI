# 从身体经验到符号：价值锚概念层的自组织形成与零样本迁移

**FakeAGI 分论文三**（总论文之外的独立实证论文）
总论文（理论基座）：[PAPER.md](PAPER.md)（十一公理体系与架构全貌）

玄幕安全团队 - guaidao2 · 墨渊安全实验室 - coolmoon · 2026-08

---

## 摘要

概念通常被视为外部标注的分类结果。本文提出并实证**价值锚概念层**：概念 = 观测簇 × 价值绑定 × 可重用——完全从身体经验自组织形成（正价值事件→"可消耗物"簇，负价值事件→"危险"簇），无需外部标签。完整管线为四阶段：**价值锚聚类 → 概念预测绑定 → 概念符号化（词-概念接地）→ 概念组合与自指**。实验显示：概念驱动行为带来 +11% 食物收益（预测绑定）；语言符号化带来 +20%（词→行为）；跨域概念零样本迁移 80%+（共享"可消耗物"抽象）；价值经决策层（概念投票）使存活率 4 倍于直控（护栏裁决）。本文给出价值锚聚类的一致性证明（价值锚 = 稳态价值信号的聚类中心）与符号接地的判别力条件。

**关键词**：概念形成；符号接地；零样本迁移；价值锚聚类；自组织

---

## 1. 引言

机器学习中的概念学习依赖标注数据（监督）或任务奖励（强化）。生物概念形成的显著特征：**概念从身体经验的"价值"中长出来**——"可食用"概念来自吃到时的观测聚类，"危险"概念来自受伤时的观测聚类——无外部标签，价值信号由稳态（homeostasis）生成。

FakeAGI 的十一公理体系将价值定位为"预测误差的加权机制"（公理③④），概念定位为"观测簇×价值绑定×可重用"（公理⑧ 符号压缩）。本文实证该概念层的完整管线及其泛化能力。

**贡献**：
1. 价值锚聚类：从稳态价值信号自组织形成概念（无标签）；
2. 概念符号化：词-概念接地（词有"所指"且影响行为）；
3. 跨域零样本迁移：共享抽象概念跨表面差异泛化；
4. 概念-决策接口：价值经决策层（概念投票）优于直控（护栏裁决）。

---

## 2. 系统概述

```
概念库（ConceptBank）：
  Concept = {质心（观测簇）, value_ema（价值绑定）, symbols（词集）}

形成机制：
  add_value_anchored(obs, v_up)：正价值事件（吃到）→ 观测并入
     "consumable" 簇（质心 = EMA 更新）
  add_danger_anchored(obs)：负价值事件（受伤）→ "danger" 簇
     （公理⑧ 正负对称压缩）
  新观测距所有簇 > 阈值 → 生成新簇（结构扩展——O(1)，无参数惰性）

使用机制：
  match_concept(obs) → (name, dist, matched, value_pred)
  bind_symbol(词) / activate_by_symbol(词)：符号-概念双向接地
  概念投票：匹配 → 委员会概念票（价值经决策层——护栏裁决）
```

**核心设计**：概念不是外部标签的存储，而是**预测单元的压缩**——概念携带价值预测（value_ema），可参与决策（投票）与语言（符号）。

---

## 3. 数学推导

### 3.1 价值锚聚类的一致性

设观测序列 $o_1, \ldots, o_T$，价值信号 $v_t \in \{+, -\}$（正/负稳态事件）。价值锚聚类将正价值观测归入簇 $C^+$：

$$C^+ = \arg\min_C \sum_{t: v_t = +} \| o_t - \mu_C \|^2$$

质心 EMA 更新：$\mu_{t+1} = \mu_t + \alpha (o_t - \mu_t)$。**一致性**：若正价值事件集中于环境中的特定区域 $R^+$（如食物位置邻域），则 $\mu \to \mathbb{E}[o | o \in R^+]$——质心收敛到正价值区域的观测中心。

**判别力条件**：概念有效的充要条件为正负价值区域在观测空间可分：

$$\min_{o \in R^+, o' \in R^-} \| o - o' \| > 2\theta$$

（$\theta$ 为匹配阈值）——否则正负簇重叠，匹配不可分（实证中 danger/consumable 区分即此条件）。

### 3.2 概念预测绑定 = 价值回归

概念的价值预测 $\hat{v}(o) = \text{value\_ema}$（该簇的历史价值均值）——等价于对"观测→价值"的局部回归：

$$\hat{v}(o) = \frac{\sum_{t: o_t \in C(o)} v_t}{|C(o)|}$$

预测绑定使匹配条件从"像"升级为"像 + 值高"（匹配阈值 + value_pred > 0.55）——**概念从聚类升级为可预测单元**（符号化前置）。

### 3.3 符号接地的判别力

符号 $w$ 与概念 $C$ 绑定（共现学习）。接地有效性：

$$P(\text{行为获益} | w) > P(\text{行为获益})$$

等价于词携带概念信息：$I(W; C) > 0$。实验 2（词→行为 +20%）为该条件的实证——词激活概念（activate_by_symbol）→ 概念引导行为 → 获益提升。

### 3.4 零样本迁移条件

域 A/B 共享概念结构 $g: \mathcal{O} \to \mathcal{S}$（符号映射）。零样本迁移成立当且仅当共享概念的判别力跨域保持：

$$\forall o \in \mathcal{O}_B: \quad g(o) \text{ 的簇归属与域 A 一致}$$

即"可消耗物"抽象在域 A（迷宫食物）、域 B（威胁场资源）、域 C（村庄果实）的表面差异下保持判别力——实验 3 的 80%+ 零样本迁移为该条件的实证。

---

## 4. 实验

### 4.1 实验 1：价值锚聚类与概念驱动（+11%）

**设计**：10×10 网格食物/水环境。对照（无概念）vs 概念驱动（价值锚聚类 + 匹配停留）。

**结果（n=10 seeds）**：

| 配置 | 食物获得 |
|------|---------|
| 对照（无概念）| 20.7 |
| 概念驱动（预测绑定）| **23.0（+11%）** |

**解读**：概念从身体经验长出（v_up 聚类）并驱动行为（匹配→停留→吃）——概念层的第一个有用闭环。

### 4.2 实验 2：概念符号化（词→行为 +20%）

**设计**：语言广播环境（说者广播方向词）——符号化开关对比。

**结果（n=10 seeds）**：

| 配置 | 食物获得 |
|------|---------|
| 无符号化 | 18.3 |
| 符号化（词-概念绑定）| **22.0（+20%）** |

**解读**：词有"所指"（绑定概念）且影响行为（词→概念激活→引导）——语言接地的量化实证。

### 4.3 实验 3：跨域概念零样本迁移（80%+）

**设计**：域 A 训练（迷宫食物）→ 零样本测域 B（威胁场资源）/域 C（村庄果实）——共享"可消耗物"概念。

**结果**：零样本迁移 80%+（第三表面判定 OK）——支持 §3.4 条件。

### 4.4 实验 4：概念-决策接口（护栏裁决——存活 4×）

**设计**：价值直控（匹配→action=0）vs 概念投票（价值经委员会加权）。

**结果（n=10 seeds）**：

| 配置 | 食物 | 存活 |
|------|------|------|
| direct（直控）| 23.0 | 1/10 |
| hint（概念投票）| 19.3 | **4/10** |

**解读**：价值经决策层仲裁不锁定行为（饿时委员会可否决停留）——存活 4×；效率略降（19.3 vs 23.0）——效率-稳健权衡，默认可切 hint。

---

## 5. 讨论

### 5.1 核心结论

1. **概念 = 自组织价值压缩**：无标签、无任务奖励——价值锚聚类从稳态信号长出概念；
2. **概念 = 可预测单元**：预测绑定（价值回归）使概念可参与决策与语言；
3. **概念 = 迁移单位**：共享抽象跨域零样本迁移 80%+——概念是符号接地的载体；
4. **概念 = 决策参与者**：价值经决策层（投票）优于直控（存活 4×）——公理③ 动机选择器语义。

### 5.2 局限（诚实）

- 观测空间低维（4-6 维方向向量）——高维视觉观测未测；
- 零样本迁移在"共享概念"设计下测——无共享结构时迁移未测（负结果可能）；
- 概念数（簇数）无自动确定机制（阈值超参）；
- 概念遗忘/合并（生命周期管理）未实现。

### 5.3 与公理体系的关系

- 公理⑧（符号压缩）——概念 = 观测簇×价值绑定×可重用（实证）；
- 公理③（动机选择器）——概念投票 = 价值经决策层（实证）；
- 公理④（精度加权）——danger/consumable 对称 = 正负价值锚（实证）。

---

## 6. 结论

价值锚概念层实现了从身体经验到符号的完整自组织管线：聚类（+11%）、预测绑定、符号化（+20%）、零样本迁移（80%+）、决策参与（存活 4×）。概念不是外部标签——它是系统对"什么值得关注"的自主压缩（公理③④⑧ 的联合实证）。

---

## 参考文献

1. Harnad, S. (1990). The symbol grounding problem. *Physica D*, 42(1-3), 335-346.
2. Lake, B. M., et al. (2017). Building machines that learn and think like people. *Behavioral and Brain Sciences*, 40, e253.
3. Friston, K. (2010). The free-energy principle: a unified brain theory? *Nature Reviews Neuroscience*, 11(2), 127-138.
4. Oudeyer, P.-Y., & Kaplan, F. (2007). What is intrinsic motivation? *Frontiers in Neurorobotics*, 1, 6.
5. Chen, T., et al. (2020). A simple framework for contrastive learning of visual representations. *ICML*.
6. Maturana, H. R., & Varela, F. J. (1980). *Autopoiesis and cognition*. Springer.
